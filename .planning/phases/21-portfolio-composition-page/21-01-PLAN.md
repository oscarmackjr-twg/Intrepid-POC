---
phase: 21-portfolio-composition-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/App.tsx
  - frontend/src/pages/ReDashboard.tsx
  - frontend/src/pages/ReExecutiveSummaryPage.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Visiting /re-dashboard shows the Executive Summary tab with KPI cards unchanged from Phase 20"
    - "Visiting /re-dashboard/portfolio shows the Portfolio tab active with an Outlet placeholder"
    - "Tab strip shows 5 tabs and navigating between them works via React Router"
    - "Stub tabs (credit, cashflow, origination) render Coming in a future phase text"
    - "recharts is installed in frontend/package.json"
  artifacts:
    - path: "frontend/src/pages/ReExecutiveSummaryPage.tsx"
      provides: "Extracted KPI card grid from ReDashboard"
      contains: "KPI_CARDS"
    - path: "frontend/src/pages/ReDashboard.tsx"
      provides: "Layout shell with tab strip and Outlet"
      contains: "Outlet"
    - path: "frontend/src/App.tsx"
      provides: "Nested route structure for /re-dashboard"
      contains: "ReExecutiveSummaryPage"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "frontend/src/pages/ReDashboard.tsx"
      via: "parent route element"
      pattern: "element.*ReDashboard"
    - from: "frontend/src/pages/ReDashboard.tsx"
      to: "Outlet"
      via: "react-router-dom Outlet"
      pattern: "<Outlet"
---

<objective>
Refactor the RE Dashboard routing into a nested layout shell and install Recharts.

Purpose: Phase 21 requires a tabbed layout where /re-dashboard is a shell with child routes. The existing KPI card content must be extracted to ReExecutiveSummaryPage.tsx so the shell can render different pages via Outlet. Recharts must be installed for chart panels in Plans 02-03.

Output: ReDashboard.tsx as layout shell with tab strip, ReExecutiveSummaryPage.tsx with extracted KPI cards, nested routes in App.tsx, recharts in package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-executive-summary-page/20-01-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/pages/ReDashboard.tsx
@frontend/src/hooks/useKPIs.ts
@frontend/src/components/re/KPICard.tsx
@frontend/src/types/re.ts

<interfaces>
<!-- From frontend/src/pages/ReDashboard.tsx (current state, will be refactored): -->
```tsx
import { ReDashboardFilterSidebar } from '../components/re/ReDashboardFilterSidebar'
import { KPICard } from '../components/re/KPICard'
import { useKPIs } from '../hooks/useKPIs'
import { formatUPB, formatRate, formatWAM, formatDSCR, formatCount } from '../utils/formatKpi'
import type { KPIResponse } from '../types/re'

const KPI_CARDS: { key: keyof KPIResponse; label: string; format: (value: number | null) => string }[]
export default function ReDashboard() // currently renders KPI grid + filter sidebar
```

<!-- From frontend/src/App.tsx (current state): -->
```tsx
<Route path="re-dashboard" element={<ReDashboard />} />
// This flat route must become a nested parent route
```

<!-- From frontend/src/hooks/useReLoanFilters.ts: -->
```tsx
export function useReLoanFilters(): { filters: ReLoanFilters; setFilter: (key, value) => void; clearFilters: () => void }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Recharts and refactor ReDashboard into layout shell with nested routes</name>
  <files>
    frontend/package.json,
    frontend/src/pages/ReDashboard.tsx,
    frontend/src/pages/ReExecutiveSummaryPage.tsx,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/pages/ReDashboard.tsx,
    frontend/src/App.tsx,
    frontend/src/hooks/useKPIs.ts,
    frontend/src/components/re/KPICard.tsx,
    frontend/src/utils/formatKpi.ts,
    frontend/package.json
  </read_first>
  <action>
**Step 1 — Install Recharts (per D-01):**

```bash
cd frontend && npm install recharts
```

Verify `recharts` appears in `frontend/package.json` dependencies after install.

**Step 2 — Create `frontend/src/pages/ReExecutiveSummaryPage.tsx` (per D-08):**

Move ALL KPI card content from `ReDashboard.tsx` into this new file. This is a verbatim extraction — preserve logic and markup exactly:

```tsx
import { useState } from 'react'
import { KPICard } from '../components/re/KPICard'
import { useKPIs } from '../hooks/useKPIs'
import { formatUPB, formatRate, formatWAM, formatDSCR, formatCount } from '../utils/formatKpi'
import type { KPIResponse } from '../types/re'

const KPI_CARDS: {
  key: keyof KPIResponse
  label: string
  format: (value: number | null) => string
}[] = [
  { key: 'total_upb',         label: 'Total UPB',          format: formatUPB },
  { key: 'wac',               label: 'WAC',                format: formatRate },
  { key: 'wam',               label: 'WAM',                format: formatWAM },
  { key: 'wa_ltv',            label: 'WA LTV',             format: formatRate },
  { key: 'wa_dscr',           label: 'WA DSCR',            format: formatDSCR },
  { key: 'active_loan_count', label: 'Active Loan Count',  format: formatCount },
  { key: 'delinquent_30_upb', label: 'Delinquent 30 UPB',  format: formatUPB },
  { key: 'delinquent_60_upb', label: 'Delinquent 60 UPB',  format: formatUPB },
  { key: 'delinquent_90_upb', label: 'Delinquent 90+ UPB', format: formatUPB },
  { key: 'portfolio_yield',   label: 'Portfolio Yield',    format: formatRate },
]

export default function ReExecutiveSummaryPage() {
  const { data, isLoading, isError } = useKPIs()
  const [highlightedDelinquency, setHighlightedDelinquency] = useState<string | null>(null)

  const isNoData = !isLoading && data !== undefined && data.active_loan_count === 0

  return (
    <>
      {isError && (
        <p className="text-sm text-red-600 mb-4">Failed to load KPI data.</p>
      )}

      <div className="grid grid-cols-2 lg:grid-cols-4 gap-4">
        {KPI_CARDS.map(({ key, label, format }) => {
          const isDelinquency = key.startsWith('delinquent_')
          const rawValue = data ? data[key] : null
          const displayValue = isNoData ? '\u2013' : format(rawValue as number | null)

          return (
            <KPICard
              key={key}
              label={label}
              value={displayValue}
              isLoading={isLoading}
              isNoData={isNoData || (!isLoading && rawValue === null)}
              isClickable={isDelinquency}
              isHighlighted={highlightedDelinquency === key}
              onClick={isDelinquency ? () => {
                setHighlightedDelinquency(prev => prev === key ? null : key)
              } : undefined}
            />
          )
        })}
      </div>
    </>
  )
}
```

Note: The outer `<div className="flex gap-6 ...">` wrapper and `<ReDashboardFilterSidebar />` stay in the shell, NOT in ReExecutiveSummaryPage. The page only renders the content that goes inside the Outlet area.

**Step 3 — Rewrite `frontend/src/pages/ReDashboard.tsx` as layout shell (per D-06, D-07, D-26):**

Replace the entire file with:

```tsx
import { NavLink, Outlet } from 'react-router-dom'
import { ReDashboardFilterSidebar } from '../components/re/ReDashboardFilterSidebar'

const TABS = [
  { label: 'Executive Summary', to: '/re-dashboard', end: true },
  { label: 'Portfolio',         to: '/re-dashboard/portfolio', end: false },
  { label: 'Credit Quality',    to: '/re-dashboard/credit', end: false },
  { label: 'Cash Flow',         to: '/re-dashboard/cashflow', end: false },
  { label: 'Origination',       to: '/re-dashboard/origination', end: false },
]

export default function ReDashboard() {
  return (
    <div className="flex gap-6 min-h-[calc(100vh-theme(spacing.12))]">
      <div className="flex-1 min-w-0">
        <h1 className="text-2xl font-bold text-[#1a3868] mb-4">RE Portfolio Dashboard</h1>

        {/* Tab strip (per D-26) */}
        <nav className="flex gap-6 border-b border-gray-200 mb-6">
          {TABS.map(({ label, to, end }) => (
            <NavLink
              key={to}
              to={to}
              end={end}
              className={({ isActive }) =>
                `pb-2 text-sm font-medium transition-colors ${
                  isActive
                    ? 'border-b-2 border-[#1a3868] text-[#1a3868]'
                    : 'text-[#475569] hover:text-[#1a3868]'
                }`
              }
            >
              {label}
            </NavLink>
          ))}
        </nav>

        {/* Child page content */}
        <Outlet />
      </div>

      {/* Filter panel — right side, always visible */}
      <ReDashboardFilterSidebar />
    </div>
  )
}
```

Key points:
- `NavLink` with `end={true}` on index tab prevents it staying active on child routes (per D-26).
- Active tab: `border-b-2 border-[#1a3868] text-[#1a3868]`. Inactive: `text-[#475569] hover:text-[#1a3868]`.
- `<Outlet />` renders the active child route page.
- Filter sidebar remains in the shell, shared across all tabs.

**Step 4 — Update `frontend/src/App.tsx` nested routes (per D-09):**

Replace the flat `<Route path="re-dashboard" element={<ReDashboard />} />` with a nested structure. Add imports for the new pages at the top:

```tsx
import ReExecutiveSummaryPage from './pages/ReExecutiveSummaryPage'
```

Create a stub component inline (or at top of file) for future tabs:

```tsx
function ReStubPage() {
  return <p className="text-[#94a3b8] text-sm mt-8">Coming in a future phase.</p>
}
```

Replace the route:
```tsx
<Route path="re-dashboard" element={<ReDashboard />}>
  <Route index element={<ReExecutiveSummaryPage />} />
  <Route path="portfolio" element={<ReStubPage />} />
  <Route path="credit" element={<ReStubPage />} />
  <Route path="cashflow" element={<ReStubPage />} />
  <Route path="origination" element={<ReStubPage />} />
</Route>
```

The `ReStubPage` placeholder for `/re-dashboard/portfolio` will be replaced with `RePortfolioPage` in Plan 02.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains "recharts" in dependencies
    - frontend/src/pages/ReExecutiveSummaryPage.tsx exists and contains "KPI_CARDS" and "useKPIs"
    - frontend/src/pages/ReDashboard.tsx contains "Outlet" and "NavLink" and does NOT contain "useKPIs" or "KPICard"
    - frontend/src/App.tsx contains "ReExecutiveSummaryPage" and 'path="portfolio"' and 'path="credit"'
    - frontend/src/App.tsx contains "ReStubPage" or equivalent stub for future tabs
    - npx tsc --noEmit exits with 0 errors
  </acceptance_criteria>
  <done>
    Visiting /re-dashboard shows KPI cards (unchanged behavior from Phase 20) under the Executive Summary tab.
    Visiting /re-dashboard/portfolio shows the Portfolio tab active with stub content.
    Tab strip renders 5 tabs with correct active/inactive styling.
    Recharts is installed and importable.
    TypeScript compiles without errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL params | User-controlled navigation via tab links — no data exposure risk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | URL route paths | accept | React Router only renders defined routes; unknown paths fall through to existing catch-all |
| T-21-02 | Information Disclosure | Tab strip labels | accept | Tab labels are static UI strings, not sensitive data |
</threat_model>

<verification>
1. `cd frontend && npx tsc --noEmit` passes with 0 errors
2. `grep -q "recharts" frontend/package.json` exits 0
3. `grep -q "Outlet" frontend/src/pages/ReDashboard.tsx` exits 0
4. `grep -q "KPI_CARDS" frontend/src/pages/ReExecutiveSummaryPage.tsx` exits 0
5. `grep -q "ReExecutiveSummaryPage" frontend/src/App.tsx` exits 0
</verification>

<success_criteria>
- ReDashboard.tsx is a layout shell (tab strip + Outlet + filter sidebar)
- ReExecutiveSummaryPage.tsx contains the extracted KPI card grid
- App.tsx has nested routes with index, portfolio, credit, cashflow, origination children
- Recharts is installed in package.json
- TypeScript compiles without errors
- No KPI card logic or behavior changes — pure refactor
</success_criteria>

<output>
After completion, create `.planning/phases/21-portfolio-composition-page/21-01-SUMMARY.md`
</output>
