---
phase: 10-revamp-user-interface-phase-10
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/index.html
  - frontend/src/index.css
  - frontend/src/assets/twg-logo.png
  - frontend/src/pages/Login.tsx
autonomous: true
requirements:
  - UI-01
  - UI-02

must_haves:
  truths:
    - "Browser tab shows 'Intrepid Loan Platform'"
    - "Gotham font stack is active site-wide via CSS"
    - "TWG navy #1a3868 is available as a CSS custom property"
    - "TWG logo PNG exists at frontend/src/assets/twg-logo.png"
    - "Login page shows 'Intrepid Loan Platform' heading in navy"
    - "Login page Sign In button is navy (#1a3868), not blue-600"
    - "Login page background is #f8fafc (very light gray)"
  artifacts:
    - path: frontend/index.html
      provides: Updated page title
      contains: "Intrepid Loan Platform"
    - path: frontend/src/index.css
      provides: Global font stack and CSS custom properties
      contains: "--color-brand"
    - path: frontend/src/assets/twg-logo.png
      provides: TWG logo for sidebar use in plan 02
    - path: frontend/src/pages/Login.tsx
      provides: Rebranded login page
      contains: "Intrepid Loan Platform"
  key_links:
    - from: frontend/src/index.css
      to: all pages
      via: "@import tailwindcss + body font-family"
      pattern: "font-family.*Gotham"
    - from: frontend/src/pages/Login.tsx
      to: StagingBanner
      via: "import StagingBanner"
      pattern: "<StagingBanner"
---

<objective>
Establish the TWG Global brand foundation across the app: update the page title, wire in Gotham font stack with brand color CSS vars, copy the logo asset, and rebrand the Login page.

Purpose: Plan 02 (Layout rewrite) depends on the logo existing at the correct path and can reference the CSS custom properties defined here.
Output: Updated index.html, index.css, Login.tsx, and the twg-logo.png asset committed to the repo.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-revamp-user-interface-phase-10/10-CONTEXT.md

<interfaces>
<!-- Key contracts for this plan. -->

From frontend/src/pages/Login.tsx (current):
- Renders <StagingBanner /> at top (must keep)
- Has "Loan Engine" heading at line 54 (rename to "Intrepid Loan Platform")
- Sign In button: bg-blue-600 hover:bg-blue-700 focus:ring-blue-500 (replace with navy)
- Outer div: bg-gray-50 (change to #f8fafc or keep bg-[#f8fafc])
- No props — self-contained page component

From frontend/src/index.css (current):
- @import "tailwindcss" on line 1
- body { font-family: -apple-system, BlinkMacSystemFont, ... } — override with Gotham stack

Logo source: C:/Users/omack/OneDrive - TWG/Pictures/TWG_logo_small.png
Logo destination: frontend/src/assets/twg-logo.png
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy logo asset and update brand globals</name>
  <files>frontend/src/assets/twg-logo.png, frontend/index.html, frontend/src/index.css</files>
  <action>
1. Create the assets directory: `mkdir -p frontend/src/assets`

2. Copy the logo: `cp "C:/Users/omack/OneDrive - TWG/Pictures/TWG_logo_small.png" frontend/src/assets/twg-logo.png`
   - If the source path does not exist (e.g. OneDrive not synced), output a clear error message and stop — do not create a placeholder. The logo must be a real file.

3. Update `frontend/index.html` — change the `<title>` from "Loan Engine" to "Intrepid Loan Platform". No other changes to index.html.

4. Replace `frontend/src/index.css` with:

```css
@import "tailwindcss";

:root {
  --color-brand: #1a3868;
  --color-brand-hover: #15305a;
  --color-page-bg: #f8fafc;
  --color-content-bg: #ffffff;
  --color-text: #1e293b;
}

body {
  margin: 0;
  font-family: 'Gotham', system-ui, -apple-system, sans-serif;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  color: var(--color-text);
  background-color: var(--color-page-bg);
}

code {
  font-family: source-code-pro, Menlo, Monaco, Consolas, 'Courier New', monospace;
}
```

Note: Gotham is not available on Google Fonts. The web fallback `system-ui, -apple-system, sans-serif` is intentional per user decision. If the TWG environment self-hosts Gotham, the font-family declaration will pick it up automatically. Do NOT attempt to load Gotham from an external CDN.
  </action>
  <verify>
    <automated>grep -l "Intrepid Loan Platform" frontend/index.html && grep -l "color-brand" frontend/src/index.css && test -f frontend/src/assets/twg-logo.png && echo "PASS"</automated>
  </verify>
  <done>
    - frontend/src/assets/twg-logo.png exists (real PNG, not placeholder)
    - frontend/index.html title is "Intrepid Loan Platform"
    - frontend/src/index.css declares --color-brand, --color-page-bg, --color-text, and Gotham font-family
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebrand Login page</name>
  <files>frontend/src/pages/Login.tsx</files>
  <action>
Update Login.tsx to apply TWG brand. Keep all logic (form submit, error handling, backend status check, StagingBanner) identical — only change visual classes and text:

1. Outer container: change `bg-gray-50` to `bg-[#f8fafc]`

2. Heading: change "Loan Engine" to "Intrepid Loan Platform". Change heading classes from `text-3xl font-extrabold text-gray-900` to `text-3xl font-bold text-[#1a3868]`

3. Sign In button: replace `bg-blue-600 hover:bg-blue-700 focus:ring-blue-500` with `bg-[#1a3868] hover:bg-[#15305a] focus:ring-[#1a3868]`

4. Input focus rings: replace `focus:ring-blue-500 focus:border-blue-500` on both input fields with `focus:ring-[#1a3868] focus:border-[#1a3868]`

5. The StagingBanner import and `<StagingBanner />` render MUST be kept exactly as-is. Do not reposition it.

Do not change: form logic, axios calls, useState hooks, error handling JSX, label text, placeholder text, backend status messages.
  </action>
  <verify>
    <automated>grep -l "Intrepid Loan Platform" frontend/src/pages/Login.tsx && grep -l "1a3868" frontend/src/pages/Login.tsx && grep -l "StagingBanner" frontend/src/pages/Login.tsx && echo "PASS"</automated>
  </verify>
  <done>
    - Login page heading reads "Intrepid Loan Platform" in navy
    - Sign In button is #1a3868 navy (not blue-600)
    - StagingBanner still imported and rendered
    - No logic changes — only class and text content changes
  </done>
</task>

</tasks>

<verification>
After both tasks:
- `grep "Intrepid Loan Platform" frontend/index.html` — returns match
- `grep "color-brand" frontend/src/index.css` — returns match
- `grep "Gotham" frontend/src/index.css` — returns match
- `grep "Intrepid Loan Platform" frontend/src/pages/Login.tsx` — returns match
- `grep "1a3868" frontend/src/pages/Login.tsx` — returns match
- `test -f frontend/src/assets/twg-logo.png` — exits 0
- `cd frontend && npm run build 2>&1 | tail -5` — build succeeds (no TypeScript/Vite errors)
</verification>

<success_criteria>
Brand foundation is in place:
- TWG logo asset committed at the correct path
- Global CSS vars and Gotham font stack declared in index.css
- Page title updated in index.html
- Login page shows TWG brand colors and "Intrepid Loan Platform" name
- Frontend build still compiles clean
</success_criteria>

<output>
After completion, create `.planning/phases/10-revamp-user-interface-phase-10/10-01-SUMMARY.md`
</output>
