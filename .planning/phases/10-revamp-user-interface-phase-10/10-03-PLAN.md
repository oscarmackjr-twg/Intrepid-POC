---
phase: 10-revamp-user-interface-phase-10
plan: "03"
type: execute
wave: 3
depends_on:
  - "10-02"
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05

must_haves:
  truths:
    - "User confirms sidebar is visible and correctly styled on every page"
    - "User confirms StagingBanner spans full width above the sidebar"
    - "User confirms app name reads 'Intrepid Loan Platform' on login and sidebar"
    - "User confirms admin-only items are hidden when logged in as non-admin"
    - "User confirms admin-only items are visible when logged in as admin"
    - "User confirms active nav item has navy left border highlight"
  artifacts:
    - path: .planning/phases/10-revamp-user-interface-phase-10/10-03-SUMMARY.md
      provides: Visual verification sign-off recorded
  key_links:
    - from: Layout.tsx sidebar
      to: all authenticated pages
      via: Outlet render
      pattern: "visual inspection"
---

<objective>
Human visual verification of the complete Phase 10 UI rebrand. Claude starts the dev server, then pauses for the user to navigate the app and confirm the visual results.

Purpose: UI changes cannot be fully verified by automated tests — a human must look at the result.
Output: User sign-off that the TWG brand sidebar matches the spec.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-revamp-user-interface-phase-10/10-CONTEXT.md
@.planning/phases/10-revamp-user-interface-phase-10/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and run build check</name>
  <files></files>
  <action>
1. Run a production build to confirm no compile errors:
   ```
   cd frontend && npm run build
   ```
   If the build fails, stop and report the TypeScript/Vite error. Do not proceed to the checkpoint.

2. Report to the user:
   - Build succeeded / failed
   - Instructions: start the dev server manually with `cd frontend && npm run dev` (or `docker compose up` if using Docker)
   - Then navigate to http://localhost:5173 (or the Docker port) to review the UI
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build exits 0 with no TypeScript errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete TWG Global brand rebrand:
- Left sidebar (240px) replacing horizontal top nav
- TWG Global logo at sidebar top
- "Intrepid Loan Platform" label under logo
- SG and CIBC non-clickable group labels with indented child items
- Active nav item: 4px navy left border + navy semibold text
- User info + Sign out at sidebar bottom
- StagingBanner above sidebar spanning full width
- Login page: "Intrepid Loan Platform" heading in navy, navy Sign In button
  </what-built>
  <how-to-verify>
Start the dev server if not already running: `cd frontend && npm run dev`

Open http://localhost:5173 and check:

1. **Login page** — heading reads "Intrepid Loan Platform" (not "Loan Engine"), Sign In button is TWG navy (dark blue), page background is light gray
2. **After login — sidebar present** — left sidebar visible at ~240px, white background, right border separator
3. **Sidebar logo** — TWG Global logo image visible at top
4. **App label** — "Intrepid Loan Platform" in small navy caps below the logo
5. **Nav items** — Dashboard, Program Runs visible; then SG group label (non-clickable, gray caps); Final Funding SG and Cash Flow SG indented below; then CIBC group label; Final Funding CIBC and Cash Flow CIBC indented below; then File Manager
6. **Active highlight** — click Dashboard → it should show a navy left border + darker text; click Program Runs → same effect
7. **Admin-only items** — if logged in as admin: Cash Flow and Holiday Maintenance appear at the bottom of the nav; if logged in as non-admin: they are absent
8. **StagingBanner** — amber "STAGING -- Not Production" bar is above the sidebar/content, spanning the full browser width
9. **Content area** — pages render correctly to the right of the sidebar; content is not obscured
10. **Browser tab** — tab title reads "Intrepid Loan Platform"
  </how-to-verify>
  <resume-signal>
Type "approved" if all items pass.
If any items fail, describe what you see (e.g., "logo not showing", "admin items missing") and Claude will fix and re-verify.
  </resume-signal>
</task>

</tasks>

<verification>
User types "approved" after visual inspection confirms all 10 checklist items pass.
</verification>

<success_criteria>
Phase 10 complete when:
- Build succeeds with no TypeScript errors
- User has visually confirmed sidebar, branding, nav groups, admin gate, StagingBanner, and Login page
- User has typed "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/10-revamp-user-interface-phase-10/10-03-SUMMARY.md`
</output>
