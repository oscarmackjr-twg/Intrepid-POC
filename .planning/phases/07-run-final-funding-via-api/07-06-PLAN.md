---
phase: 07-run-final-funding-via-api
plan: 06
type: execute
wave: 2
depends_on:
  - "07-02"
  - "07-03"
  - "07-04"
  - "07-05"
files_modified:
  - .github/workflows/deploy-test.yml
autonomous: false
requirements:
  - HARD-05

must_haves:
  truths:
    - "deploy-test.yml has a security-quality-gate job that runs before deploy"
    - "The deploy job has needs: [security-quality-gate] blocking it"
    - "security-quality-gate runs: ruff, mypy, pip-audit, npm audit, terraform validate"
    - "TruffleHog secret scanning step is included in security-quality-gate"
    - "Human confirms the job structure looks correct by reading the workflow YAML"
  artifacts:
    - path: ".github/workflows/deploy-test.yml"
      provides: "security-quality-gate job blocking deploy"
      contains: "security-quality-gate"
  key_links:
    - from: ".github/workflows/deploy-test.yml:security-quality-gate"
      to: ".github/workflows/deploy-test.yml:deploy"
      via: "needs: [security-quality-gate]"
      pattern: "needs:.*security-quality-gate"
---

<objective>
Add the blocking `security-quality-gate` CI job to deploy-test.yml (HARD-05). This job runs ruff, mypy, pip-audit, npm audit, terraform validate, and TruffleHog before any deploy can proceed. Followed by a human checkpoint to confirm the CI structure is correct before merging.

Purpose: Enforce code quality and dependency security as a merge gate — prevents regressions and known CVEs from reaching staging.
Output: Updated GitHub Actions workflow with blocking security gate; human-verified structure.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-run-final-funding-via-api/07-05-SUMMARY.md

<interfaces>
<!-- Current deploy-test.yml structure (from research): -->
<!--   Line 27: existing deploy job — needs: [] currently -->
<!--   Job names and structure should be read from file before editing -->

<!-- CI tools to add (all available as pip install in ubuntu-latest): -->
<!--   ruff >= 0.4.0     — ruff check backend/ && ruff format --check backend/ -->
<!--   mypy >= 1.10.0    — mypy backend/ --ignore-missing-imports --no-strict-optional --exclude venv -->
<!--   pip-audit >= 2.7.0 — pip-audit -r backend/requirements.txt -->
<!--   npm audit          — npm ci && npm audit --audit-level=high (in frontend/) -->
<!--   terraform validate — terraform init -backend=false && terraform validate (in deploy/terraform/qa/) -->
<!--   truffleHog         — trufflesecurity/trufflehog-actions-scan@main (GitHub Actions) -->

<!-- Job name must be: security-quality-gate (per user decision in CONTEXT.md) -->
<!-- Deploy job must add: needs: [security-quality-gate] -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security-quality-gate job to deploy-test.yml</name>
  <files>.github/workflows/deploy-test.yml</files>
  <action>
Read `.github/workflows/deploy-test.yml` in full first to understand current job structure.

Add the `security-quality-gate` job. Insert it BEFORE the `deploy` job in the YAML. The job must be named exactly `security-quality-gate`:

```yaml
  security-quality-gate:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0   # TruffleHog needs full history

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install CI tools
        run: pip install ruff mypy pip-audit

      - name: Ruff lint
        run: ruff check backend/

      - name: Ruff format check
        run: ruff format --check backend/

      - name: Mypy type check
        run: mypy backend/ --ignore-missing-imports --no-strict-optional --exclude venv --exclude migrations

      - name: pip-audit CVE scan
        run: pip-audit -r backend/requirements.txt

      - name: npm audit
        working-directory: frontend
        run: |
          npm ci
          npm audit --audit-level=high

      - name: Terraform validate
        working-directory: deploy/terraform/qa
        run: |
          terraform init -backend=false
          terraform validate

      - name: TruffleHog secret scan
        uses: trufflesecurity/trufflehog-actions-scan@main
        with:
          path: ./
          base: ${{ github.event.repository.default_branch }}
          head: HEAD
          extra_args: --debug --only-verified
```

After adding the job, find the `deploy` job in the file and add `needs: [security-quality-gate]` to it. If `deploy` already has a `needs:` field, append `security-quality-gate` to the list.

Preserve all existing content — only add the new job and modify the `needs:` field on deploy.

Validate the YAML syntax:
```bash
cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc && python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-test.yml'))" && echo "YAML valid"
```
  </action>
  <verify>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc && python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-test.yml')); print('YAML valid')" 2>&1</automated>
  </verify>
  <done>YAML is valid; deploy-test.yml contains security-quality-gate job; deploy job has needs: [security-quality-gate]</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
All seven hardening areas implemented across Plans 01-06:
- HARD-07: app-bundle.zip removed from git, .gitignore updated
- HARD-01: Terraform RDS private subnets, ALB HTTPS listener, SG egress tightened
- HARD-02: SECRET_KEY startup guard, seed script one-time passwords, password policy validator
- HARD-04: Sanitized error responses with correlation IDs, no file:// URIs
- HARD-06: AuditLog DB model + Alembic migration + DB write in audit.py
- HARD-03: HttpOnly cookie auth (login sets cookie, AuthContext uses withCredentials, rate limiting, CSP header)
- HARD-05: security-quality-gate CI job blocking deploy
  </what-built>
  <how-to-verify>
1. Open `.github/workflows/deploy-test.yml` and verify:
   - `security-quality-gate` job exists with all 8 steps (ruff, mypy, pip-audit, npm audit, terraform validate, TruffleHog)
   - `deploy` job has `needs: [security-quality-gate]`

2. Run the full backend test suite locally:
   ```
   cd backend && pytest tests/ -q -m "not integration"
   ```
   All tests should pass GREEN (no red).

3. Verify git index is clean:
   ```
   git ls-files deploy/aws/eb/app-bundle.zip
   ```
   Must return empty.

4. Check Terraform validates:
   ```
   cd deploy/terraform/qa && terraform validate
   ```
   Must return "Success! The configuration is valid."

5. Check key security changes:
   - `backend/auth/routes.py` — find `set_cookie` and `@limiter.limit`
   - `frontend/src/contexts/AuthContext.tsx` — confirm zero `localStorage` references
   - `backend/config/settings.py` — find `LOCAL_DEV_MODE` and `validate_secret_key`
   - `backend/db/models.py` — find `class AuditLog`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<verification>
The checkpoint:human-verify task IS the verification for this plan. Before the checkpoint:
```bash
cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc
# Backend tests all GREEN
cd backend && pytest tests/ -q -m "not integration" 2>&1 | tail -10
# YAML valid
python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy-test.yml')); print('YAML valid')"
# Git index clean
git ls-files deploy/aws/eb/app-bundle.zip
```
</verification>

<success_criteria>
- deploy-test.yml has security-quality-gate job with all required steps
- deploy job blocked by needs: [security-quality-gate]
- YAML passes syntax validation
- Human has approved all seven hardening areas via checkpoint
- All backend tests GREEN
</success_criteria>

<output>
After completion, create `.planning/phases/07-run-final-funding-via-api/07-06-SUMMARY.md`
</output>
