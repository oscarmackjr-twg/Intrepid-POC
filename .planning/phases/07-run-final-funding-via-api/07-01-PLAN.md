---
phase: 07-run-final-funding-via-api
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/test_settings_guard.py
  - backend/tests/test_seed_admin.py
  - backend/tests/test_auth_security.py
  - backend/tests/test_api_files.py
  - backend/tests/test_storage_local.py
  - backend/tests/test_audit_log.py
  - backend/tests/test_auth_routes.py
autonomous: true
requirements:
  - HARD-02
  - HARD-03
  - HARD-04
  - HARD-06

must_haves:
  truths:
    - "All Wave 0 test files exist and fail (RED) before any implementation starts"
    - "pytest collects the new tests without import errors"
    - "Each test targets a specific behavior that does not yet exist in the codebase"
  artifacts:
    - path: "backend/tests/test_settings_guard.py"
      provides: "Tests for SECRET_KEY startup guard (HARD-02)"
      exports: ["test_startup_fails_with_fallback_secret_key", "test_startup_succeeds_with_local_dev_mode"]
    - path: "backend/tests/test_seed_admin.py"
      provides: "Tests for one-time password generation (HARD-02)"
      exports: ["test_generates_non_hardcoded_password", "test_password_is_url_safe_string"]
    - path: "backend/tests/test_auth_security.py"
      provides: "Tests for cookie token extraction in get_current_user (HARD-03)"
      exports: ["test_get_current_user_accepts_cookie_token", "test_get_current_user_rejects_no_token"]
    - path: "backend/tests/test_api_files.py"
      provides: "Tests for sanitized error responses in files API (HARD-04)"
      exports: ["test_list_files_error_returns_generic_message", "test_error_response_contains_correlation_id"]
    - path: "backend/tests/test_storage_local.py"
      provides: "Tests for file:// URL removal in local storage (HARD-04)"
      exports: ["test_get_file_url_returns_api_path_not_file_uri"]
    - path: "backend/tests/test_audit_log.py"
      provides: "Tests for AuditLog DB table schema and write (HARD-06)"
      exports: ["test_audit_log_table_schema", "test_audit_log_db_write_on_login"]
  key_links:
    - from: "backend/tests/test_settings_guard.py"
      to: "backend/config/settings.py"
      via: "imports Settings and tests validation"
      pattern: "from config.settings import Settings"
    - from: "backend/tests/test_audit_log.py"
      to: "backend/auth/audit.py"
      via: "imports audit functions"
      pattern: "from auth.audit import log_user_action"
---

<objective>
Create failing (RED) test scaffolds for all behaviors that Phase 7 implementation plans must make pass. Six new test files plus extensions to the existing test_auth_routes.py.

Purpose: Nyquist compliance — every implementation task in Plans 02-05 must have a failing test before any code is written.
Output: Seven test files containing specific failing assertions gating HARD-02, HARD-03, HARD-04, and HARD-06.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Existing test infrastructure (from backend/pytest.ini and conftest.py pattern) -->
<!-- Test DB: sqlite:///:memory: — SQLite-compatible types only in models -->
<!-- pytest.ini: testpaths = tests, default marker: not integration -->
<!-- Quick run: cd backend && pytest tests/ -x -q -m "not integration" -->

From backend/config/settings.py (line 52):
  SECRET_KEY: str = "your-secret-key-change-in-production"  # sentinel value

From backend/auth/audit.py (existing signature — logger-only today):
  def log_user_action(action: str, user, ...): ...

From backend/auth/security.py:
  async def get_current_user(token: str = Depends(oauth2_scheme), db: Session = Depends(get_db)) -> User: ...

From backend/api/files.py (lines to fix in Plan 03):
  44:  detail=f"Failed to list files: {str(e)}"
  97:  detail=f"Failed to upload file: {detail}"
  165: detail=f"Failed to get file URL: {str(e)}"

From backend/storage/local.py (line 103 to fix in Plan 03):
  return f"file://{file_path.as_uri()}"

From backend/scripts/seed_admin.py (to fix in Plan 02):
  27: password: str = "admin123"
  181: default_password = "twg123"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create failing test scaffolds for HARD-02 (secrets and passwords)</name>
  <files>backend/tests/test_settings_guard.py, backend/tests/test_seed_admin.py, backend/tests/test_auth_routes.py</files>
  <action>
Create `backend/tests/test_settings_guard.py`:
- Import `Settings` from `config.settings`
- `test_startup_fails_with_fallback_secret_key`: instantiate `Settings(SECRET_KEY="your-secret-key-change-in-production", LOCAL_DEV_MODE=False)` inside `pytest.raises(Exception)` — expect a `ValueError` or `ValidationError`. This test MUST fail (pass) currently because the guard does not exist yet. Actually write it so it FAILS at run time now: assert that Settings raises when called with the sentinel. Since the guard doesn't exist yet, Settings will not raise — so the test will fail (red).
- `test_startup_succeeds_with_local_dev_mode`: instantiate `Settings(SECRET_KEY="your-secret-key-change-in-production", LOCAL_DEV_MODE=True)` — assert no exception raised.

Create `backend/tests/test_seed_admin.py`:
- Mock or import `seed_admin` carefully to test the password generation logic without running full DB code
- `test_generates_non_hardcoded_password`: run the password generation code (extract the function or test the module-level logic) and assert the result is NOT "admin123" and NOT "twg123"
- `test_password_is_url_safe_string`: assert the generated password is at least 16 chars and does not contain spaces

Add to `backend/tests/test_auth_routes.py` (append, do not overwrite):
- `test_password_policy_rejects_short_password`: POST to `/api/auth/register` (or equivalent) with password shorter than 12 chars — expect 422
- `test_password_policy_rejects_no_uppercase`: POST register with `"alllowercase1234"` — expect 422
- `test_password_policy_rejects_no_digit`: POST register with `"AllUppercaseOnly"` — expect 422
- `test_password_policy_accepts_strong_password`: POST register with `"StrongPass1234!"` — expect not 422 on validation (may be 400/409 for other reasons, but not 422 for password)

Use the existing `client` fixture from conftest.py. Tests should fail (RED) because password policy validation does not exist yet.
  </action>
  <verify>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && pytest tests/test_settings_guard.py tests/test_seed_admin.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>New test files exist; pytest collects them without import errors; tests fail (RED) because the guards/validators they test don't exist yet</done>
</task>

<task type="auto">
  <name>Task 2: Create failing test scaffolds for HARD-03, HARD-04, and HARD-06</name>
  <files>backend/tests/test_auth_security.py, backend/tests/test_api_files.py, backend/tests/test_storage_local.py, backend/tests/test_audit_log.py, backend/tests/test_auth_routes.py</files>
  <action>
Create `backend/tests/test_auth_security.py`:
- `test_get_current_user_accepts_cookie_token`: call a protected endpoint using a TestClient with a cookie `access_token=Bearer {valid_jwt}` in the request cookies (not Authorization header) — expect 200 not 401. FAILS now because get_current_user only reads Authorization header.
- `test_get_current_user_rejects_no_token`: call a protected endpoint with neither cookie nor header — expect 401.
- `test_rate_limit_applied_to_login` (can stub): assert login endpoint module has `@limiter.limit` decorator applied — inspect via `client.post("/api/auth/login", ...)` 11 times and assert at least one returns 429. Will fail now because rate limiter is not installed.

Add to `backend/tests/test_auth_routes.py` (further extend):
- `test_login_sets_httponly_cookie`: call `POST /api/auth/login` with valid credentials — assert response has `Set-Cookie` header containing `HttpOnly`. FAILS now because login returns JSON token only.
- `test_login_does_not_return_token_in_body`: same call — assert response JSON does NOT contain `"access_token"` key. FAILS now.

Create `backend/tests/test_api_files.py`:
- Use TestClient with authenticated request
- `test_list_files_error_returns_generic_message`: patch `storage.list_files` to raise an exception — assert response detail does NOT contain the raw exception string, DOES contain "ref:" (correlation ID pattern). FAILS now because files.py returns `str(e)`.
- `test_error_response_contains_correlation_id`: same patch — assert the correlation ID in the response detail matches a UUID pattern `[0-9a-f-]{36}`.

Create `backend/tests/test_storage_local.py`:
- `test_get_file_url_returns_api_path_not_file_uri`: instantiate `LocalStorage(base_path=tmp_path)`, create a temp file, call `get_file_url(filename)` — assert the return value does NOT start with `"file://"`. FAILS now because local.py returns `file://` URI.

Create `backend/tests/test_audit_log.py`:
- `test_audit_log_table_schema`: using the test DB session (sqlite in-memory), call `Base.metadata.create_all(engine)` and assert the `audit_log` table exists with columns: `id`, `event_type`, `user_id`, `timestamp`, `source_ip`, `resource`, `outcome`, `detail_json`. FAILS now because AuditLog model does not exist.
- `test_audit_log_db_write_on_login`: mock a login success — call `log_user_action(...)` — query the `audit_log` table and assert one row was inserted. FAILS now because `log_user_action` does not write to DB.

Keep tests self-contained; use `unittest.mock.patch` where external services would be called.
  </action>
  <verify>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && pytest tests/test_auth_security.py tests/test_api_files.py tests/test_storage_local.py tests/test_audit_log.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>All six new test files created; pytest collects all tests without ImportError; all newly added tests fail RED (implementation does not exist yet)</done>
</task>

</tasks>

<verification>
Run full scaffold verification:
```
cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && pytest tests/test_settings_guard.py tests/test_seed_admin.py tests/test_auth_security.py tests/test_api_files.py tests/test_storage_local.py tests/test_audit_log.py -v --co 2>&1 | tail -40
```
All test IDs should be collected. The existing test suite should still pass (no regressions from adding new test files).
</verification>

<success_criteria>
- Seven test files exist (6 new + extensions to test_auth_routes.py)
- `pytest --collect-only` collects all new tests without ImportError
- New tests are FAILING (RED) — proving implementation does not exist
- Existing passing tests continue to pass (no regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/07-run-final-funding-via-api/07-01-SUMMARY.md`
</output>
