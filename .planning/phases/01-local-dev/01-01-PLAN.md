---
phase: 01-local-dev
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/.env
  - backend/.env.example
  - backend/.gitignore
autonomous: true
requirements:
  - LOCAL-03
  - LOCAL-04

must_haves:
  truths:
    - "backend/.env contains STORAGE_TYPE=local as the active setting"
    - "backend/.env contains no hardcoded Windows absolute paths (no C:\\ occurrences)"
    - "backend/.env.example exists and is committed to git"
    - "backend/.env.example has LOCAL DEV section at top (active) and S3/PRODUCTION section commented out below"
    - "backend/.env.example sets DEV_INPUT=./data/sample/files_required with explanatory comment"
    - "backend/.gitignore excludes backend/.env"
  artifacts:
    - path: "backend/.env"
      provides: "Cleaned local dev env — STORAGE_TYPE=local, no Windows paths"
    - path: "backend/.env.example"
      provides: "Self-documenting env template for developer onboarding"
      exports: ["DATABASE_URL", "STORAGE_TYPE", "SECRET_KEY", "DEV_INPUT", "S3_BUCKET_NAME"]
  key_links:
    - from: "backend/.env"
      to: "backend/config/settings.py"
      via: "pydantic-settings env_file='.env'"
      pattern: "STORAGE_TYPE=local"
    - from: "backend/.env.example"
      to: "backend/.env"
      via: "developer copies and fills in"
      pattern: "DATABASE_URL=postgresql"
---

<objective>
Clean backend/.env to remove all hardcoded Windows paths and set STORAGE_TYPE=local as the active default. Create backend/.env.example as a self-documenting onboarding template. Verify .gitignore properly excludes backend/.env.

Purpose: These two files are the primary integration point — every other plan in this phase depends on a clean env config. Without this, the backend cannot start with local Postgres and the storage layer uses S3 instead of local disk.
Output: backend/.env (cleaned, gitignored), backend/.env.example (committed template)
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-local-dev/01-CONTEXT.md
@.planning/phases/01-local-dev/01-RESEARCH.md

<interfaces>
<!-- Key patterns from backend/config/settings.py — no changes needed to this file -->
<!-- pydantic-settings loads backend/.env when cwd=backend/ -->
<!-- model_config: env_file=".env", extra="ignore", env_ignore_empty=True -->
<!-- DATABASE_URL can be a full connection string OR built from individual components -->
<!-- DEV_INPUT: points at files_required/ dir; settings.apply_dev_paths() derives INPUT_DIR as parent -->
<!--   DEV_INPUT=./data/sample/files_required → INPUT_DIR=./data/sample -->
<!--   Pipeline appends files_required/ back — so DEV_INPUT must include /files_required suffix -->

<!-- From backend/storage/factory.py: -->
<!-- STORAGE_TYPE=local → LocalStorageBackend -->
<!-- STORAGE_TYPE=s3   → S3StorageBackend (requires S3_BUCKET_NAME, S3_REGION, etc.) -->

<!-- Current backend/.env state (to be corrected): -->
<!-- STORAGE_TYPE=s3  (active — WRONG for local dev) -->
<!-- DEV_INPUT=C:\Users\omack\...  (Windows absolute path — must be removed) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean backend/.env and create backend/.env.example</name>
  <files>backend/.env, backend/.env.example</files>
  <action>
Read the current backend/.env first to see its full contents before rewriting.

Rewrite backend/.env with this exact structure (two clearly separated sections):

```
# backend/.env (gitignored — do not commit)
# Copy from .env.example and fill in your values.

## LOCAL DEV
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/intrepid_poc
STORAGE_TYPE=local
SECRET_KEY=local-dev-secret-key-change-in-production

# Optional: point at a specific local files_required directory.
# Defaults to backend/data/inputs/ when unset.
# Uncomment and set to use sample data out of the box:
# DEV_INPUT=./data/sample/files_required
# DEV_OUTPUT=
# DEV_OUTPUT_SHARED=

## S3 / PRODUCTION (comment these back in when using S3)
# STORAGE_TYPE=s3
# S3_BUCKET_NAME=
# S3_REGION=us-east-1
# S3_INPUT=input/files_required
# S3_OUTPUT=outputs
# S3_OUTPUT_SHARED=output_share
# AWS_PROFILE=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
```

Critical rules:
- STORAGE_TYPE=local must be the ACTIVE (uncommented) line
- No line may contain C:\ or any Windows absolute path
- DEV_INPUT, DEV_OUTPUT, DEV_OUTPUT_SHARED must be left commented out (per user decision)
- S3 vars must be present but all commented out

Then create backend/.env.example with the same structure but with DEV_INPUT=./data/sample/files_required uncommented (so new developers point at sample data by default):

```
# backend/.env.example — committed template
# Copy this file to backend/.env and fill in your values.
# See DEVELOPMENT.md for full setup instructions.

## LOCAL DEV
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/intrepid_poc
STORAGE_TYPE=local
# SECRET_KEY: generate a strong random key for local dev. Any string works locally.
SECRET_KEY=local-dev-secret-key-change-in-production

# DEV_INPUT: path to your files_required directory (the pipeline's input folder).
# Points at the committed sample dataset by default — run with --pdate 2026-02-19.
# Override with your own real files_required directory to use live data.
DEV_INPUT=./data/sample/files_required
# DEV_OUTPUT=      # Optional: override local output directory
# DEV_OUTPUT_SHARED=  # Optional: override shared output directory

## S3 / PRODUCTION
# Uncomment and fill these in when deploying to staging/production.
# STORAGE_TYPE=s3
# S3_BUCKET_NAME=      # Your S3 bucket name
# S3_REGION=us-east-1  # AWS region
# S3_INPUT=input/files_required   # S3 prefix for input files
# S3_OUTPUT=outputs               # S3 prefix for output files
# S3_OUTPUT_SHARED=output_share   # S3 prefix for shared output files
# AWS_PROFILE=         # AWS CLI profile (local only — not used in ECS)
# AWS_ACCESS_KEY_ID=   # Set via Secrets Manager in production
# AWS_SECRET_ACCESS_KEY=
```
  </action>
  <verify>
    <automated>grep -n "C:\\\\" /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend/.env && echo "FAIL: Windows paths found" || echo "PASS: No Windows paths in .env"</automated>
    <automated>grep -n "^STORAGE_TYPE=local" /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend/.env && echo "PASS: STORAGE_TYPE=local is active" || echo "FAIL: STORAGE_TYPE=local not found as active setting"</automated>
    <automated>test -f /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend/.env.example && echo "PASS: .env.example exists" || echo "FAIL: .env.example missing"</automated>
  </verify>
  <done>backend/.env has STORAGE_TYPE=local active, no C:\ paths, DEV_INPUT commented out. backend/.env.example exists with DEV_INPUT=./data/sample/files_required set by default.</done>
</task>

<task type="auto">
  <name>Task 2: Verify .gitignore excludes backend/.env</name>
  <files>backend/.gitignore</files>
  <action>
Read backend/.gitignore. The research confirms line 37 already has `.env` excluded. Verify the exclusion covers `backend/.env` (not just `.env` in root). The .gitignore in backend/ uses relative paths, so `.env` in backend/.gitignore excludes `backend/.env` correctly.

Check if `.env` appears in the gitignore file. If it already exists (confirmed in research), no change is needed — document the verification. If for any reason it is missing, add:

```
# Environment variables
.env
.env.local
.env.*.local
```

Also verify that backend/.env.example is NOT in .gitignore (it must be committable). If .env.example appears in .gitignore, remove that line.

Run git check-ignore to confirm:
```bash
cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc
git check-ignore -v backend/.env
git check-ignore -v backend/.env.example
```

backend/.env should be ignored. backend/.env.example should NOT be ignored (no output from git check-ignore means it is trackable).
  </action>
  <verify>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc && git check-ignore -v backend/.env && echo "PASS: backend/.env is gitignored" || echo "FAIL: backend/.env is NOT gitignored"</automated>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc && git check-ignore -v backend/.env.example && echo "FAIL: .env.example is gitignored (should not be)" || echo "PASS: .env.example is trackable"</automated>
  </verify>
  <done>backend/.env is gitignored. backend/.env.example is not gitignored and can be committed.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `grep -rn "C:\\\\" backend/.env` returns no output (no Windows paths)
2. `grep "^STORAGE_TYPE=local" backend/.env` returns a match
3. `backend/.env.example` exists and contains both `## LOCAL DEV` and `## S3 / PRODUCTION` sections
4. `git check-ignore -v backend/.env` shows it is ignored
5. `git check-ignore -v backend/.env.example` returns nothing (trackable)
</verification>

<success_criteria>
- backend/.env: STORAGE_TYPE=local active, no hardcoded Windows paths, S3 vars commented out (LOCAL-03)
- backend/.env.example: exists, self-documenting, DEV_INPUT=./data/sample/files_required default, S3 block commented (LOCAL-04 partial)
- backend/.gitignore: .env is excluded, .env.example is trackable
</success_criteria>

<output>
After completion, create .planning/phases/01-local-dev/01-01-SUMMARY.md
</output>
