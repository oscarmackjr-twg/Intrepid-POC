---
phase: 01-local-dev
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - backend/.gitignore
  - backend/data/sample/files_required/MASTER_SHEET.xlsx
  - backend/data/sample/files_required/MASTER_SHEET - Notes.xlsx
  - backend/data/sample/files_required/current_assets.csv
  - backend/data/sample/files_required/Underwriting_Grids_COMAP.xlsx
  - backend/data/sample/files_required/Tape20Loans_02-18-2026.csv
  - backend/data/sample/files_required/SFY_02-18-2026_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx
  - backend/data/sample/files_required/PRIME_02-18-2026_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx
  - backend/migrations/versions/<hash>_initial_schema.py
autonomous: true
requirements:
  - LOCAL-05
  - LOCAL-06

must_haves:
  truths:
    - "backend/data/sample/files_required/ exists and is tracked by git (not gitignored)"
    - "Sample data contains all required pipeline input files dated 2026-02-18"
    - "backend/migrations/versions/ contains at least one migration file"
    - "alembic upgrade head exits 0 against local intrepid_poc Postgres database"
  artifacts:
    - path: "backend/data/sample/files_required/"
      provides: "Minimal pipeline input dataset for local dev onboarding"
    - path: "backend/migrations/versions/"
      provides: "Initial schema migration covering all SQLAlchemy models"
      contains: "initial schema"
  key_links:
    - from: "backend/.env"
      to: "backend/migrations/env.py"
      via: "settings.DATABASE_URL injected at migration runtime"
      pattern: "DATABASE_URL=postgresql"
    - from: "backend/data/sample/files_required/"
      to: "backend/orchestration/pipeline.py"
      via: "DEV_INPUT env var → settings.apply_dev_paths() → INPUT_DIR"
      pattern: "files_required/MASTER_SHEET.xlsx"
---

<objective>
Create synthetic sample data in backend/data/sample/files_required/ and generate the initial Alembic migration from existing SQLAlchemy models. Update backend/.gitignore to allow committing the sample directory while keeping data/ generally excluded.

Purpose: LOCAL-06 (pipeline runs end-to-end locally) requires sample data that matches the pipeline's expected file naming conventions. LOCAL-05 (Alembic migrations apply cleanly) requires the initial migration revision to exist. Both tasks are independent and can be done in any order but are grouped here as Wave 2 foundation artifacts.
Output: Committed sample dataset, generated migration file, updated .gitignore with exception rules
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-local-dev/01-CONTEXT.md
@.planning/phases/01-local-dev/01-RESEARCH.md
@.planning/phases/01-local-dev/01-01-SUMMARY.md

<interfaces>
<!-- Pipeline file loading requirements (from backend/orchestration/pipeline.py): -->
<!--   load_reference_data() expects under {INPUT_DIR}/files_required/: -->
<!--     MASTER_SHEET.xlsx -->
<!--     MASTER_SHEET - Notes.xlsx -->
<!--     current_assets.csv -->
<!--     Underwriting_Grids_COMAP.xlsx (with many sheets) -->
<!--   discover_input_files() matches by date pattern from pdate: -->
<!--     Tape20Loans_{yesterday}.csv              (yesterday = pdate - 1 day) -->
<!--     SFY_{yesterday}_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx -->
<!--     PRIME_{yesterday}_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx -->

<!-- CRITICAL: pdate=2026-02-19 → yesterday=2026-02-18 → filename date=02-18-2026 -->
<!-- So sample files must be dated 02-18-2026 (not 02-19-2026) -->

<!-- DEV_INPUT path handling (settings.py apply_dev_paths): -->
<!--   DEV_INPUT=./data/sample/files_required -->
<!--   → INPUT_DIR = ./data/sample  (parent stripped) -->
<!--   → pipeline reads: {INPUT_DIR}/files_required/MASTER_SHEET.xlsx -->
<!--   → which resolves to: ./data/sample/files_required/MASTER_SHEET.xlsx ✓ -->

<!-- SQLAlchemy models to migrate (backend/db/models.py): -->
<!--   users, sales_teams, pipeline_runs, loan_exceptions, loan_facts, holidays -->
<!--   Enums: UserRole, RunStatus (will generate CREATE TYPE in Postgres) -->

<!-- Existing backend/.gitignore excludes: -->
<!--   data/ -->
<!--   *.xlsx -->
<!--   *.csv -->
<!-- Must add negation rules AFTER the data/ exclusion line for sample/ to be tracked -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create synthetic sample data and update .gitignore exception</name>
  <files>backend/.gitignore, backend/data/sample/files_required/MASTER_SHEET.xlsx, backend/data/sample/files_required/MASTER_SHEET - Notes.xlsx, backend/data/sample/files_required/current_assets.csv, backend/data/sample/files_required/Underwriting_Grids_COMAP.xlsx, backend/data/sample/files_required/Tape20Loans_02-18-2026.csv, backend/data/sample/files_required/SFY_02-18-2026_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx, backend/data/sample/files_required/PRIME_02-18-2026_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx</files>
  <action>
First, read backend/tests/conftest.py and backend/orchestration/pipeline.py to understand the minimum data structure the pipeline expects. This determines what synthetic sample files must contain.

Then read the actual files in backend/files_required/ to understand the exact sheet names and column headers required by the pipeline. IMPORTANT: Do NOT copy these files into sample/ — they contain real financial data. Use the structure as a reference only to build minimal synthetic equivalents.

Create the directory: backend/data/sample/files_required/

Write a Python script at backend/scripts/create_sample_data.py that generates all required sample files using openpyxl and csv modules (already available in the Python environment). Run the script to generate the files. The script should:

1. Create MASTER_SHEET.xlsx — minimal loan master sheet with the columns the pipeline reads (inspect pipeline.py load_master_sheet() to find required columns). Use 3-5 synthetic loan rows with fake but structurally valid data.

2. Create MASTER_SHEET - Notes.xlsx — notes variant; inspect what columns are needed.

3. Create current_assets.csv — minimal CSV with headers matching what load_reference_data() expects.

4. Create Underwriting_Grids_COMAP.xlsx — must have the following sheet names (pipeline requires all of them): SFY, Prime, SFY - Notes, Prime - Notes, SFY COMAP, SFY COMAP2, Prime COMAP, Notes CoMAP, SFY COMAP-Oct25, SFY COMAP-Oct25-2, Prime CoMAP-Oct25, Prime CoMAP-Oct25-2, Prime CoMAP - New. Each sheet can be minimal (headers only or a few rows) — just enough for the pipeline not to crash.

5. Create Tape20Loans_02-18-2026.csv — loan tape CSV. Date in filename is 02-18-2026 (yesterday when pdate=2026-02-19). Include the columns the pipeline's tape loader expects. Use 3-5 synthetic loan rows.

6. Create SFY_02-18-2026_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx — SFY exhibit file. Date 02-18-2026. Minimal content matching what the pipeline reads.

7. Create PRIME_02-18-2026_ExhibitAtoFormofSaleNotice - Pre-Funding.xlsx — PRIME exhibit file. Same date pattern.

After generating files, update backend/.gitignore to add negation rules AFTER the existing `data/` exclusion line. Read the current .gitignore first to find the exact line number of `data/`, then add these rules immediately after:

```gitignore
# Exception: commit sample data for local dev onboarding
!data/sample/
!data/sample/**
!data/sample/**/*.xlsx
!data/sample/**/*.csv
```

CRITICAL: The negation rules must appear AFTER the `data/` and `*.xlsx` and `*.csv` exclusion lines — git processes rules in order, and later negations override earlier exclusions. You cannot un-ignore a file inside an already-ignored directory without un-ignoring the parent directory first (`!data/sample/` must precede `!data/sample/**`).

After updating .gitignore, run:
```bash
cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc
git check-ignore -v backend/data/sample/files_required/current_assets.csv
```
This must return nothing (file is trackable). If it still shows as ignored, check that the negation rules appear after ALL applicable exclusion patterns.
  </action>
  <verify>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc && git check-ignore -v backend/data/sample/files_required/current_assets.csv && echo "FAIL: sample CSV still gitignored" || echo "PASS: sample CSV is trackable"</automated>
    <automated>ls /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend/data/sample/files_required/ | wc -l && echo "sample files listed above (expect 7)"</automated>
    <automated>test -f /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend/data/sample/files_required/Tape20Loans_02-18-2026.csv && echo "PASS: tape file exists" || echo "FAIL: tape file missing"</automated>
  </verify>
  <done>backend/data/sample/files_required/ exists with 7 synthetic sample files. backend/.gitignore allows tracking sample/. git check-ignore confirms sample CSV is trackable.</done>
</task>

<task type="auto">
  <name>Task 2: Generate initial Alembic migration</name>
  <files>backend/migrations/versions/<hash>_initial_schema.py</files>
  <action>
Ensure local Postgres has the intrepid_poc database. If it does not exist, create it:
```bash
psql -U postgres -c "CREATE DATABASE intrepid_poc;" 2>/dev/null || echo "Database may already exist"
```

Generate the initial Alembic migration from existing SQLAlchemy models:
```bash
cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend
source venv/bin/activate || venv/Scripts/activate 2>/dev/null || true
alembic revision --autogenerate -m "initial schema"
```

This generates a file in backend/migrations/versions/<hash>_initial_schema.py. Review the generated file to confirm it includes:
- CREATE TYPE statements for UserRole and RunStatus Postgres enums (must come before table DDL)
- CREATE TABLE for: users, sales_teams, pipeline_runs, loan_exceptions, loan_facts, holidays

Then apply the migration:
```bash
alembic upgrade head
```

This must exit 0. If it fails:
- If tables already exist from a previous `Base.metadata.create_all()` call, drop and recreate the database:
  ```bash
  psql -U postgres -c "DROP DATABASE intrepid_poc; CREATE DATABASE intrepid_poc;"
  alembic upgrade head
  ```
- If the venv is not yet set up, run `pip install -r requirements.txt` first

Do NOT modify alembic.ini — it correctly has no sqlalchemy.url set; migrations/env.py reads settings.DATABASE_URL at runtime.

The generated migration file in migrations/versions/ must be committed (it is not gitignored).
  </action>
  <verify>
    <automated>ls /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend/migrations/versions/ | grep -v "^$" | wc -l && echo "migration files listed above (expect >= 1)" || echo "FAIL: no migration files found"</automated>
    <automated>cd /c/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && venv/bin/alembic current 2>&1 && echo "PASS: alembic can read migration state" || venv/Scripts/alembic.exe current 2>&1 || echo "FAIL: alembic current failed"</automated>
  </verify>
  <done>backend/migrations/versions/ contains an initial_schema migration file. alembic upgrade head exits 0. alembic current shows head revision.</done>
</task>

</tasks>

<verification>
After both tasks:
1. `ls backend/data/sample/files_required/` shows 7 files including Tape20Loans_02-18-2026.csv
2. `git check-ignore -v backend/data/sample/files_required/Tape20Loans_02-18-2026.csv` returns nothing (trackable)
3. `ls backend/migrations/versions/` shows at least one .py file
4. `cd backend && venv/bin/alembic upgrade head` exits 0 (or confirms already at head)
5. `cd backend && venv/bin/alembic current` shows the head revision hash
</verification>

<success_criteria>
- Sample data exists and is trackable in git (LOCAL-06 infrastructure ready)
- Alembic initial migration exists and applies cleanly to local Postgres (LOCAL-05)
- backend/.gitignore exception rules are in correct order (after data/ exclusion)
</success_criteria>

<output>
After completion, create .planning/phases/01-local-dev/01-02-SUMMARY.md
</output>
