---
phase: 23-cash-flow-performance-page
plan: 03
type: execute
wave: 3
depends_on:
  - 23-01
  - 23-02
files_modified: []
autonomous: false
requirements:
  - CASHFLOW-01
  - CASHFLOW-02
  - CASHFLOW-03
  - CASHFLOW-04
  - CASHFLOW-05
  - UX-01
must_haves:
  truths:
    - "All five cash flow panels render with real data (not empty state)"
    - "Applying a sidebar filter visibly updates all five panels"
    - "P&I chart shows two distinct lines (actual and projected)"
    - "Yield analysis metric cards show non-zero numeric values"
  artifacts:
    - path: ".planning/phases/23-cash-flow-performance-page/23-03-SUMMARY.md"
      provides: "Human sign-off with visual confirmation of all five panels"
      contains: "APPROVED"
  key_links:
    - from: "human verifier"
      to: "http://localhost:5173/re-dashboard/cashflow"
      via: "browser navigation to Cash Flow tab"
      pattern: "visual inspection"
---

<objective>
Run the backend test suite, verify TypeScript compiles, start the dev server, and have a human visually confirm all five cash flow panels render correctly with seeded data.

Purpose: Automated checks verify code correctness; visual verification confirms charts render with real data at the correct routes and that filter reactivity works end-to-end.
Output: Human sign-off on all five panels. Phase 23 complete.
</objective>

<execution_context>
@C:\Users\omack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-cash-flow-performance-page/23-01-SUMMARY.md
@.planning/phases/23-cash-flow-performance-page/23-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated checks</name>
  <files></files>
  <action>
Run all automated checks in sequence. All must pass before proceeding to the human checkpoint.

1. Backend contract tests:
   ```
   cd backend && python -m pytest tests/test_re_api.py::test_cashflow_performance_contract tests/test_re_api.py::test_cashflow_performance_no_loans_returns_empty tests/test_re_api.py::test_market_context_contract -v -q
   ```

2. Full backend test suite (no regressions):
   ```
   cd backend && python -m pytest tests/test_re_api.py -x -q
   ```

3. TypeScript compile check:
   ```
   cd frontend && npx tsc --noEmit
   ```

4. Structural checks:
   - `grep "ReCashFlowPage" frontend/src/App.tsx` — must return import and route (not ReStubPage)
   - `grep "re-cashflow-performance" frontend/src/pages/ReCashFlowPage.tsx` — must match queryKey
   - `grep "re-market-context" frontend/src/pages/ReCashFlowPage.tsx` — must match queryKey
   - `grep -c "Number(" frontend/src/pages/ReCashFlowPage.tsx` — must return >= 5 (Decimal coercion)

5. Start local dev server (if not already running):
   - Backend: `cd backend && uvicorn main:app --reload --port 8000`
   - Frontend: `cd frontend && npm run dev`
   - Navigate to http://localhost:5173/re-dashboard/cashflow

Report any failures before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_re_api.py -x -q 2>&1 | tail -5 && cd ../frontend && npx tsc --noEmit 2>&1 | head -10</automated>
  </verify>
  <done>All backend tests pass, TypeScript compiles clean, dev server running, cashflow page reachable at /re-dashboard/cashflow.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
ReCashFlowPage with five panels:
1. P&I Actual vs Projected — full-width LineChart with two series (navy actual, grey dashed projected)
2. NOI Trend — half-width BarChart of monthly net operating income
3. CPR Trend — half-width LineChart of constant prepayment rate percentage
4. Yield Analysis — full-width metric card row: Gross Yield, Net Yield, SOFR Spread, Treasury Spread
5. Loss and Recovery — full-width metric card row: Net Loss Rate, Realized Losses, Recovery (stub $0)

Route: /re-dashboard/cashflow (replaces "Coming in a future phase." stub)
Filter reactivity: sidebar filter changes update all panels via useReLoanFilters()
  </what-built>
  <how-to-verify>
1. Log in at http://localhost:5173 (admin / IntrepidStaging2024!)
2. Navigate to RE Dashboard → Cash Flow tab
3. Verify the page shows FIVE panels (not "Coming in a future phase.")
4. Panel 1 (P&I Actual vs Projected): confirm two line series are visible with a legend showing "Actual P&I" and "Projected P&I"; hover a data point to see tooltip with dollar amounts
5. Panel 2 (NOI Trend): confirm bar chart shows 12 monthly bars with dollar amounts on hover
6. Panel 3 (CPR Trend): confirm line chart shows CPR percentage values; 0.00% is acceptable if actuals match scheduled
7. Panel 4 (Yield Analysis): confirm four metric cards show numeric values — Gross Yield, Net Yield, SOFR Spread (+/- bps), Treasury Spread (+/- bps); values should NOT be em-dash (—) unless there are truly no cashflow periods
8. Panel 5 (Loss and Recovery): confirm three metric cards show Net Loss Rate (0.000% is acceptable), Realized Losses (dollar amount), Recovery ($0 with stub label)
9. Filter reactivity: apply a sidebar filter (e.g., select a Property Type) — confirm all five panels update/reload without page navigation
10. Remove the filter — confirm panels restore to full-portfolio view

Expected values (approximate, from seeded data):
- P&I line chart: 12 months of data points
- NOI bars: positive dollar values each month
- Yield Analysis: Gross Yield ~5-8%, SOFR Spread and Treasury Spread in bps (positive expected for above-market yield)
  </how-to-verify>
  <resume-signal>Type "approved" if all five panels render correctly with data. Describe any issues if panels show empty state, errors, or missing data.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verifier browser -> local dev server | Local only — no production data at risk during visual verification |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-08 | Information Disclosure | Human verification session | accept | Local dev environment only; seeded synthetic data; no production credentials used during visual verification |
</threat_model>

<verification>
1. Backend test suite passes: `cd backend && python -m pytest tests/test_re_api.py -x -q`
2. TypeScript compiles: `cd frontend && npx tsc --noEmit`
3. All five panels visible at /re-dashboard/cashflow with seeded data
4. Filter sidebar updates trigger re-fetch on all panels
5. Human approval recorded in 23-03-SUMMARY.md
</verification>

<success_criteria>
- All automated checks pass (backend tests green, TypeScript clean)
- Human confirms all five panels render with data (not empty state / stub text)
- P&I chart shows two distinct line series
- Yield analysis metric cards show numeric values
- Filter reactivity confirmed end-to-end
- Phase 23 complete — /re-dashboard/cashflow is no longer a stub
</success_criteria>

<output>
After completion, create `.planning/phases/23-cash-flow-performance-page/23-03-SUMMARY.md`
</output>
