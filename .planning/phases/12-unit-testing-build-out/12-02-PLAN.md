---
phase: 12-unit-testing-build-out
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_cashflow_amortization.py
  - backend/tests/test_cashflow_waterfall.py
  - backend/tests/test_cashflow_prepayment.py
  - backend/tests/test_rules_comap.py
  - backend/tests/test_orchestration_archive.py
autonomous: true
requirements: [TEST-02, TEST-03, TEST-04]

must_haves:
  truths:
    - "Cashflow amortization, waterfall, and prepayment modules have unit test coverage"
    - "CoMAP grid lookup, oct25_cutoff skip logic, and program-absent skip logic are tested"
    - "Archive run date derivation and path construction are tested"
    - "All new tests pass and are marked @pytest.mark.unit"
    - "No new tests require DB, file I/O, or network"
  artifacts:
    - path: "backend/tests/test_cashflow_amortization.py"
      provides: "Amortization engine unit tests"
      contains: "pytest.mark.unit"
    - path: "backend/tests/test_cashflow_waterfall.py"
      provides: "Waterfall model unit tests"
      contains: "pytest.mark.unit"
    - path: "backend/tests/test_cashflow_prepayment.py"
      provides: "Prepayment model unit tests"
      contains: "pytest.mark.unit"
    - path: "backend/tests/test_rules_comap.py"
      provides: "CoMAP grid rule tests"
      contains: "pytest.mark.unit"
    - path: "backend/tests/test_orchestration_archive.py"
      provides: "Archive run path/date logic tests"
      contains: "pytest.mark.unit"
  key_links:
    - from: "backend/tests/test_cashflow_amortization.py"
      to: "backend/cashflow/compute/amortization.py"
      via: "direct function imports"
      pattern: "from cashflow\\.compute\\.amortization import"
    - from: "backend/tests/test_cashflow_waterfall.py"
      to: "backend/cashflow/compute/waterfall.py"
      via: "direct function imports"
      pattern: "from cashflow\\.compute\\.waterfall import"
    - from: "backend/tests/test_cashflow_prepayment.py"
      to: "backend/cashflow/compute/prepayment.py"
      via: "direct function imports"
      pattern: "from cashflow\\.compute\\.prepayment import"
    - from: "backend/tests/test_rules_comap.py"
      to: "backend/rules/comap.py"
      via: "direct function and constant imports"
      pattern: "from rules\\.comap import"
    - from: "backend/tests/test_orchestration_archive.py"
      to: "backend/orchestration/archive_run.py"
      via: "direct function imports"
      pattern: "from orchestration\\.archive_run import"
---

<objective>
Add unit test coverage for the 5 untested modules: cashflow amortization, waterfall, prepayment, rules/comap, and orchestration/archive_run.

Purpose: These are the biggest untested gaps in the codebase. Cashflow compute modules are revenue-critical. CoMAP grid logic has subtle skip-vs-flag behavior that needs test protection.
Output: 5 new test files, all passing, all marked `@pytest.mark.unit`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unit-testing-build-out/12-CONTEXT.md
@.planning/phases/12-unit-testing-build-out/12-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cashflow compute tests (amortization, waterfall, prepayment)</name>
  <files>
    backend/tests/test_cashflow_amortization.py,
    backend/tests/test_cashflow_waterfall.py,
    backend/tests/test_cashflow_prepayment.py
  </files>
  <read_first>
    backend/cashflow/compute/amortization.py,
    backend/cashflow/compute/waterfall.py,
    backend/cashflow/compute/prepayment.py,
    backend/tests/conftest.py,
    backend/pytest.ini
  </read_first>
  <behavior>
    **Amortization (test_cashflow_amortization.py):**
    - Test: level_pay_schedule final balance is zero (or near-zero within floating point)
    - Test: level_pay_schedule period count matches input term
    - Test: level_pay_schedule constant payment amount across all periods
    - Test: zero interest rate produces equal principal payments with zero interest
    - Test: edge case — single period loan
    - Test: negative or zero principal raises ValueError (if applicable, check source)

    **Waterfall (test_cashflow_waterfall.py):**
    - Test: senior tranche receives interest before junior tranche
    - Test: principal allocation respects priority ordering
    - Test: single tranche receives all cashflows
    - Test: zero available cashflow produces zero allocations
    - Test: edge case — more tranches than available cashflow

    **Prepayment (test_cashflow_prepayment.py):**
    - Test: cpr_to_smm(0.0) returns 0.0
    - Test: cpr_to_smm(1.0) returns 1.0 (100% CPR)
    - Test: psa_speed at month 30+ returns 6% CPR (PSA 100% plateau)
    - Test: psa_speed ramp — month 1 returns 0.2% (6% / 30)
    - Test: psa_speed with different PSA multipliers (e.g., 200% PSA)
    - Test: cpr_to_smm mid-range values (e.g., 6% CPR)
  </behavior>
  <action>
**Per D-05, D-06, D-07:** Create 3 new test files for the cashflow compute modules. All tests must be pure unit tests — no DB, no file I/O, no network. Mark every test class or function with `@pytest.mark.unit`.

**Step 1:** Read each source module to identify all public functions, their signatures, and return types. The research doc provides examples but the actual function signatures may differ — always verify against the source.

**Step 2:** Create `backend/tests/test_cashflow_amortization.py`:
- Import the public functions from `cashflow.compute.amortization`
- Use `pytest.approx()` for all floating-point comparisons
- Use inline scalar/list inputs — no conftest fixtures needed (per Claude's discretion: inline fixtures for cashflow, not shared conftest)
- Organize tests in classes by function name (e.g., `TestLevelPaySchedule`)

**Step 3:** Create `backend/tests/test_cashflow_waterfall.py`:
- Import from `cashflow.compute.waterfall`
- Build small tranche/cashflow dicts inline
- Test priority ordering, zero-cashflow edge case, single-tranche passthrough

**Step 4:** Create `backend/tests/test_cashflow_prepayment.py`:
- Import from `cashflow.compute.prepayment`
- Test `cpr_to_smm` conversion boundary values (0, 1, mid-range)
- Test PSA speed ramp and plateau

**Important:** If source modules have different function names than expected by the research doc, use the actual names. Read the source first.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_cashflow_amortization.py tests/test_cashflow_waterfall.py tests/test_cashflow_prepayment.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_cashflow_amortization.py` exists and contains `pytest.mark.unit`
    - `backend/tests/test_cashflow_waterfall.py` exists and contains `pytest.mark.unit`
    - `backend/tests/test_cashflow_prepayment.py` exists and contains `pytest.mark.unit`
    - `pytest tests/test_cashflow_amortization.py tests/test_cashflow_waterfall.py tests/test_cashflow_prepayment.py` exits 0
    - No imports of `sqlalchemy`, `httpx`, `TestClient`, or file I/O (`open(`, `pathlib`) in these 3 files
    - Each file has at least 4 test functions
  </acceptance_criteria>
  <done>3 cashflow compute test files exist with pure unit tests covering amortization, waterfall, and prepayment. All pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoMAP rules + archive run tests</name>
  <files>
    backend/tests/test_rules_comap.py,
    backend/tests/test_orchestration_archive.py
  </files>
  <read_first>
    backend/rules/comap.py,
    backend/orchestration/archive_run.py,
    backend/tests/conftest.py
  </read_first>
  <behavior>
    **CoMAP (test_rules_comap.py, per D-08, D-09):**
    - Test: _prog_in_grid returns True when program exists in a FICO band column
    - Test: _prog_in_grid returns False when program absent from ALL grid columns (skip, not flag)
    - Test: FICO band lookup matches correct column based on score range
    - Test: oct25_cutoff logic — all loans use oct25 grids (cutoff date 1900-10-24)
    - Test: SFY vs PRIME routing uses correct grid constants
    - Test: Program present in one column but not others still returns True

    **Archive (test_orchestration_archive.py, per D-10):**
    - Test: _is_s3_style_prefix rejects absolute Windows path (C:/Users/...)
    - Test: _is_s3_style_prefix rejects absolute Unix path (/home/...)
    - Test: _is_s3_style_prefix accepts S3-style key (runs/run_abc/output)
    - Test: _is_s3_style_prefix rejects empty string and None
    - Test: _collect_input_paths finds files in files_required subdirectory (using temp_dir)
    - Test: date derivation logic for archive path construction
  </behavior>
  <action>
**CoMAP tests (per D-08, D-09):**

Create `backend/tests/test_rules_comap.py`:
- Import the internal helpers from `rules.comap`: `_prog_in_grid`, `_found_in_grid`, and the FICO band constant dicts (`SFY_COMAP_COLS_MIN_FICO`, `PRIME_COMAP_COLS_MIN_FICO2`, etc.)
- Build synthetic grid DataFrames INLINE in each test function (per D-09 — do NOT use conftest `sample_comap_df`, it's too sparse for edge cases)
- Grid column names MUST exactly match the keys in `SFY_COMAP_COLS_MIN_FICO` / `PRIME_COMAP_COLS_MIN_FICO2` — import these dicts and use their keys
- Test the "absent from all columns" skip logic: create a grid with programs that do NOT include the tested program name, assert `_prog_in_grid` returns False
- Test the "present in one column" case: program in only the 660-699 column, assert returns True
- Mark all tests `@pytest.mark.unit`

**CRITICAL pitfall (from research):** If test grid column names don't match the `fico_col_mins` dict keys, `avail` will be empty and all lookups return False regardless of data. Always use the imported constant dict keys as column names.

**Archive tests (per D-10):**

Create `backend/tests/test_orchestration_archive.py`:
- Import `_is_s3_style_prefix` and `_collect_input_paths` from `orchestration.archive_run`
- For `_is_s3_style_prefix`: test with Windows absolute paths (`C:/Users/data`), Unix absolute paths (`/home/user`), S3-style keys (`runs/run_abc/output`), empty string, and None
- For `_collect_input_paths`: use the `temp_dir` fixture from conftest.py. Create `temp_dir/files_required/` with dummy files, then call `_collect_input_paths` and assert the file names appear in the result
- For date derivation: test the archive path construction logic — read `archive_run.py` to identify the function that builds the date-based archive path, and test with known date inputs
- Mark all tests `@pytest.mark.unit`
- Per D-11: do NOT test `s3_input_sync.py`
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_rules_comap.py tests/test_orchestration_archive.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `backend/tests/test_rules_comap.py` exists and contains `pytest.mark.unit`
    - `backend/tests/test_orchestration_archive.py` exists and contains `pytest.mark.unit`
    - `test_rules_comap.py` contains at least one test with `_prog_in_grid` and the word "absent" or "skip" in a test name
    - `test_rules_comap.py` does NOT import `sample_comap_df` from conftest
    - `test_orchestration_archive.py` contains `_is_s3_style_prefix`
    - `pytest tests/test_rules_comap.py tests/test_orchestration_archive.py` exits 0
    - Each file has at least 4 test functions
  </acceptance_criteria>
  <done>CoMAP grid rules and archive path logic have unit test coverage. All tests pass. Inline grid fixtures used for CoMAP (not conftest).</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest tests/test_cashflow_amortization.py tests/test_cashflow_waterfall.py tests/test_cashflow_prepayment.py tests/test_rules_comap.py tests/test_orchestration_archive.py -v --tb=short` all pass
- All 5 new test files contain `@pytest.mark.unit`
- No new test file imports DB, HTTP client, or file-writing utilities
</verification>

<success_criteria>
5 new test files created, all passing, all pure unit tests. Cashflow compute, CoMAP rules, and archive logic now have test coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/12-unit-testing-build-out/12-02-SUMMARY.md`
</output>
