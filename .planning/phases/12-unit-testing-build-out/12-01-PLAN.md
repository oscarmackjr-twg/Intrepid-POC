---
phase: 12-unit-testing-build-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/test_normalize.py
  - backend/tests/test_scheduler.py
  - backend/tests/test_integration_pipeline.py
  - backend/tests/test_rules_purchase_price.py
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "All 10 previously-failing tests now pass"
    - "pytest -m 'not integration' exits 0 with no failures and no errors"
    - "No production code was modified — only test code changed"
  artifacts:
    - path: "backend/tests/test_normalize.py"
      provides: "Fixed header-row-skipping assertions"
      contains: "test_header_row_skipping"
    - path: "backend/tests/test_scheduler.py"
      provides: "Scheduler teardown isolation"
      contains: "scheduler.shutdown"
    - path: "backend/tests/test_integration_pipeline.py"
      provides: "In-memory DataFrame pipeline tests"
      contains: "sample_buy_df"
    - path: "backend/tests/test_rules_purchase_price.py"
      provides: "Fixed exception assertion"
      contains: "test_exception_generation"
  key_links:
    - from: "backend/tests/test_scheduler.py"
      to: "scheduler.job_scheduler.scheduler"
      via: "shutdown() in finally blocks"
      pattern: "scheduler\\.shutdown"
    - from: "backend/tests/test_integration_pipeline.py"
      to: "backend/tests/conftest.py"
      via: "synthetic DataFrame fixtures"
      pattern: "sample_buy_df|sample_sfy_df|sample_prime_df"
---

<objective>
Fix all 10 currently-failing/erroring tests across 4 test files to get the suite fully green.

Purpose: The test suite must be green before adding new coverage (Plan 02) or wiring CI (Plan 03). These are all test-code-only fixes — no production code changes.
Output: 4 updated test files, all passing under `pytest -m "not integration"`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-unit-testing-build-out/12-CONTEXT.md
@.planning/phases/12-unit-testing-build-out/12-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix 4 failing test files (normalize, scheduler, integration pipeline, purchase price)</name>
  <files>
    backend/tests/test_normalize.py,
    backend/tests/test_scheduler.py,
    backend/tests/test_integration_pipeline.py,
    backend/tests/test_rules_purchase_price.py
  </files>
  <read_first>
    backend/tests/test_normalize.py,
    backend/tests/test_scheduler.py,
    backend/tests/test_integration_pipeline.py,
    backend/tests/test_rules_purchase_price.py,
    backend/transforms/normalize.py,
    backend/rules/purchase_price.py,
    backend/tests/conftest.py,
    backend/pytest.ini
  </read_first>
  <action>
**Step 0 — Capture exact failure messages:** Run `cd backend && python -m pytest tests/test_normalize.py tests/test_scheduler.py tests/test_integration_pipeline.py tests/test_rules_purchase_price.py -v --tb=long 2>&1 | head -200` to see exact assertion errors before making any edits.

**Fix 1 — test_normalize.py (3 failures, per D-01):**
The normalize implementation (`normalize_sfy_df`, `normalize_prime_df`) uses `df.iloc[4:]` to skip the first 4 rows, then promotes the first remaining row as column headers. With a 5-row input, that leaves 0 data rows after header promotion. Fix the 3 failing tests:
- `TestNormalizeSfyDf::test_header_row_skipping`: Update the test DataFrame to have 6+ rows (4 junk rows + 1 header row + 1+ data rows), or update the `assert len(result) == ...` to match actual output. Run the function against the fixture first to confirm the actual length.
- `TestNormalizeSfyDf::test_tu144_column_standardization`: Same pattern — ensure enough rows for data to survive the skip+header-promotion.
- `TestNormalizePrimeDf::test_header_row_skipping`: Same fix as the SFY variant.

Do NOT modify `backend/transforms/normalize.py` — the implementation is authoritative per D-01.

**Fix 2 — test_scheduler.py (3 failures, per D-02):**
The scheduler singleton (`from scheduler.job_scheduler import scheduler`) leaks state between tests. Fix the 3 failing tests:
- `TestScheduleDailyRuns::test_schedule_daily_runs_with_teams`: Add `scheduler.remove_all_jobs()` before the test body and in a `finally` block after.
- `TestSchedulerIntegration::test_scheduler_startup`: Add `if scheduler.running: scheduler.shutdown()` at the start of the test (defensive cleanup from prior tests). Keep `scheduler.shutdown()` in `finally`.
- `TestSchedulerIntegration::test_scheduler_job_management`: Same defensive startup check. Ensure `scheduler.shutdown()` in `finally`.

Use a pytest fixture with `yield` if cleaner:
```python
@pytest.fixture(autouse=True)
def clean_scheduler():
    if scheduler.running:
        scheduler.shutdown()
    scheduler.remove_all_jobs()
    yield
    if scheduler.running:
        scheduler.shutdown()
    scheduler.remove_all_jobs()
```

Keep real APScheduler — no mocking per D-02.

**Fix 3 — test_integration_pipeline.py (2 errors, per D-03):**
Replace file-based fixture (`sample_input_dir`) with synthetic in-memory DataFrames. The tests `test_pipeline_execution` and `test_pipeline_with_exceptions` fail because file fixtures are not found at runtime.
- Patch the file discovery/loading layer to return `sample_buy_df`, `sample_sfy_df`, `sample_prime_df` from conftest.
- The test verifies orchestration logic, not file I/O.
- If the pipeline's `run_pipeline()` function reads files directly, mock the file-reading functions to return the conftest DataFrames.
- Mark these tests as `@pytest.mark.integration` if they depend on DB; otherwise keep as unit.

**Fix 4 — test_rules_purchase_price.py (1 failure, per D-04):**
Fix `TestGetPurchasePriceExceptions::test_exception_generation`:
- Run the test with `-v -s` first to see the exact `AssertionError`.
- Read `backend/rules/purchase_price.py` to confirm the exact keys in the exception dict (`seller_loan_number`, `exception_type`, `severity`, `message`, `exception_category`, `loan_data`).
- Update the assertion to use the correct key names. If the test uses `exceptions[0]['loan_number']`, change to `exceptions[0]['seller_loan_number']`. If the `message` assertion is the issue, check the f-string in `purchase_price.py` and match.
  </action>
  <verify>
    <automated>cd backend && python -m pytest tests/test_normalize.py tests/test_scheduler.py tests/test_integration_pipeline.py tests/test_rules_purchase_price.py -v --tb=short 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `pytest tests/test_normalize.py` exits 0 with 0 failures
    - `pytest tests/test_scheduler.py` exits 0 with 0 failures
    - `pytest tests/test_integration_pipeline.py` exits 0 with 0 errors
    - `pytest tests/test_rules_purchase_price.py` exits 0 with 0 failures
    - No files under `backend/transforms/`, `backend/rules/`, `backend/orchestration/`, `backend/scheduler/` were modified (git diff shows only test file changes)
  </acceptance_criteria>
  <done>All 10 previously-failing tests pass. Only test files modified, zero production code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Full suite green check</name>
  <files>none (verification only)</files>
  <read_first>backend/pytest.ini</read_first>
  <action>
Run the full test suite (excluding integration-marked tests per pytest.ini default) and confirm zero failures/errors:

```bash
cd backend && python -m pytest -m "not integration" --tb=short -q
```

If any OTHER tests are now broken (regression from the fixes), diagnose and fix. The scheduler fixture changes in particular could affect test_scheduler.py's passing tests if the autouse fixture interferes.

Confirm total test count is reasonable (should be 50+ tests given the 25 test files).
  </action>
  <verify>
    <automated>cd backend && python -m pytest -m "not integration" --tb=short -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -m "not integration"` output contains "passed" and does not contain "failed" or "error"
    - Exit code is 0
  </acceptance_criteria>
  <done>Full test suite is green with zero failures and zero errors.</done>
</task>

</tasks>

<verification>
- `cd backend && python -m pytest -m "not integration" --tb=short -q` exits 0
- `git diff --name-only` shows only files under `backend/tests/` (no production code)
</verification>

<success_criteria>
All 10 previously-failing tests pass. Full suite green. No production code modified.
</success_criteria>

<output>
After completion, create `.planning/phases/12-unit-testing-build-out/12-01-SUMMARY.md`
</output>
