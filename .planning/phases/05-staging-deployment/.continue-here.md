---
phase: 05-staging-deployment
task: 1
total_tasks: 3
status: in_progress
last_updated: 2026-03-09T03:09:37Z
---

<current_state>
Phase 5, Plan 03 — first live staging deploy. Plans 01 (StagingBanner) and 02 (seed script) are complete and committed. Plan 03 Task 1 is in progress: we made several backend pipeline bug fixes while testing the deploy, and all fixes are committed to main. The next action is to push to origin/main and trigger the GitHub Actions deploy pipeline.
</current_state>

<completed_work>

- Phase 3 (AWS infra): Complete — all INFRA-01–04 verified
- Phase 4 (CI/CD): Complete — deploy-test.yml workflow wired, all 04-0x plans done
- Phase 5 Plan 01: Done — StagingBanner component, VITE_APP_ENV build arg in Dockerfile + GitHub Actions (`757fa63`)
- Phase 5 Plan 02: Done — `scripts/seed_staging_user.py`, `docs/CICD.md` first-deploy checklist (`d6d6d66`)
- Phase 5 Plan 03 Task 1 (in progress):
  - Backend pipeline fixes made and committed during testing:
    - `c17bd4d` — escape `%` chars in DATABASE_URL for configparser
    - `d95c0f1` — NaN FICO/term handling, missing promo_term column, pandas fillna deprecation
    - `38a2c56` — CoMAP applied-for-program fallback, missing grid coverage
    - `b0437b7` — align underwriting and CoMAP checks with reference notebook
    - `b479506` — always write comap_not_passed.xlsx; fix summary route crash
  - All changes committed; nothing staged or modified (git status shows only untracked temp files)
</completed_work>

<remaining_work>

- Task 1: Push to origin/main → trigger GitHub Actions deploy-test.yml → confirm workflow success + ECS service stable (runningCount=1)
- Task 2: Run admin seed script via ECS one-off task (`scripts/seed_staging_user.py`); confirm exit code 0 and CloudWatch log shows "Admin user created/updated: admin"
- Task 3 (human checkpoint): Open ALB URL in browser; verify amber banner + admin login + file upload on all pages
- After Task 3 approved: write `05-03-SUMMARY.md` and mark Phase 5 complete
</remaining_work>

<decisions_made>

- All five backend pipeline fixes were done on main directly (no feature branch) — they are part of the Phase 5 first-deploy stabilization work
- `%` in password `Intrepid456$%` must be URL-encoded as `%25` in postgresql:// connection strings (psql, configparser, alembic)
- CoMAP skip logic: programs absent from all grid columns are skipped (not flagged) — per reference notebook alignment
- `comap_not_passed.xlsx` is always written even when empty (header-only) to prevent missing-file errors downstream
</decisions_made>

<blockers>
- None known. All backend pipeline bug fixes are committed. Deploy has not been re-triggered since the last fix (`b479506`).
</blockers>

<context>
We were working through Phase 5 Plan 03 (first live staging deploy). The deploy-test.yml workflow likely ran, but the backend pipeline had several bugs surfaced during testing (NaN values, percent-encoding, CoMAP grid coverage, etc.). All bugs are now fixed and committed to main. The codebase is clean — no uncommitted changes, only untracked temp/scratch files (backend/temp_sfy_check.py, temp.ps1, sg-rules.json, etc.) that can be ignored.

The ALB URL is: http://intrepid-poc-qa-alb-1332245107.us-east-1.elb.amazonaws.com
Admin creds (after seed): admin / IntrepidStaging2024!

ECS cluster: intrepid-poc-qa, region: us-east-1
Terraform state in: deploy/terraform/qa/ (needed for seed task subnet/SG IDs)
</context>

<next_action>
Start with: `git push origin main` from the repo root, then run `gh run list --workflow=deploy-test.yml --limit=3` to monitor the pipeline. If the latest run is already green and post-date of the last fix (b479506), proceed to Task 2 (seed script).
</next_action>
