---
phase: 11-refing-ui-for-regression-testing
plan: "05"
type: execute
wave: 3
depends_on: ["11-01", "11-02", "11-03", "11-04"]
files_modified: []
autonomous: false
requirements: [UI-06, UI-07, REG-01, REG-02]

must_haves:
  truths:
    - "All nav active state items function correctly — only one item highlights at a time"
    - "Program Runs and File Manager pages are visually clean at typical screen widths"
    - "REGRESSION_TEST.md checklist has been executed (dry-run) and results recorded"
    - "Regression script runs without crashing on the TestData directory"
  artifacts:
    - path: "docs/REGRESSION_TEST.md"
      provides: "Completed dry-run checklist with Claude's results filled in"
  key_links:
    - from: "docs/REGRESSION_TEST.md"
      to: "https://qa.oscarmackjr.com"
      via: "Ops QA sign-off section"
      pattern: "QA sign-off"
---

<objective>
Human verification checkpoint: Claude performs the REGRESSION_TEST.md dry-run against the local dev server, then hands off to Ops for QA sign-off.

Purpose: Confirm all UI fixes from Plans 01-02 are visually correct, then confirm regression infrastructure (Plans 03-04) is ready for Ops to use.

Output: Completed dry-run section in REGRESSION_TEST.md; Ops performs QA sign-off against https://qa.oscarmackjr.com.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-refing-ui-for-regression-testing/11-CONTEXT.md
@.planning/phases/11-refing-ui-for-regression-testing/11-01-SUMMARY.md
@.planning/phases/11-refing-ui-for-regression-testing/11-02-SUMMARY.md
@.planning/phases/11-refing-ui-for-regression-testing/11-03-SUMMARY.md
@.planning/phases/11-refing-ui-for-regression-testing/11-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Claude dry-run — build verification and checklist execution</name>
  <files>docs/REGRESSION_TEST.md</files>
  <action>
Run the following verification steps and update `docs/REGRESSION_TEST.md` with results:

1. **Build the frontend**: `cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/frontend && npm run build`. If it fails, stop and report the error.

2. **Verify nav active-state fixes**: Read `frontend/src/components/Layout.tsx` and confirm each nav link has a unique active condition. Check:
   - Program Runs: `!location.search.includes('type=')` condition present
   - Final Funding SG: `type=sg` condition present
   - Final Funding CIBC: `type=cibc` condition present
   - Cash Flow SG: `type=sg` + cashflow path condition present
   - Cash Flow CIBC: `type=cibc` + cashflow path condition present

3. **Verify layout restructure**: Read `frontend/src/pages/ProgramRuns.tsx` and `frontend/src/pages/FileManager.tsx`. Confirm:
   - `max-w-5xl` on outer wrapper in both files
   - File list JSX appears before upload zone JSX in FileManager.tsx (check line numbers)

4. **Verify regression script syntax**: `python -c "import ast; ast.parse(open('backend/scripts/regression_test.py').read()); print('SYNTAX OK')"`

5. **Verify REGRESSION_TEST.md completeness**: Check it contains all 7 sections (Authentication, Sidebar Navigation, Dashboard, Program Runs, File Manager, Core Ops Workflow, Visual/Brand) and Sign-Off section.

6. **Update REGRESSION_TEST.md**: Fill in the "Local dry-run (Claude)" sign-off line with today's date and PASS or FAIL based on the above checks. Add brief notes for any items that could not be verified programmatically (e.g. visual items).
  </action>
  <verify>
    <automated>cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/frontend && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>Frontend build passes. REGRESSION_TEST.md has Claude dry-run date filled in. All code-verifiable checklist items confirmed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plan 01: Nav active-state bug fixes + typography polish (Layout.tsx, index.css)
    Plan 02: Page layout restructure — ProgramRuns.tsx and FileManager.tsx with max-w-5xl and section reordering
    Plan 03: docs/REGRESSION_TEST.md manual checklist
    Plan 04: backend/scripts/regression_test.py data regression harness
  </what-built>
  <how-to-verify>
    1. Start the frontend dev server: `cd frontend && npm run dev` (or use the built app at localhost:8000 if backend is running).

    2. Open the app in a browser. Work through REGRESSION_TEST.md sections 1-7:
       - Section 2 (Sidebar Nav): Click each nav item and confirm ONLY that item highlights.
       - Section 4 (Program Runs): Confirm content is constrained (not full-width), cards have comfortable padding.
       - Section 5 (File Manager): Confirm file list appears above upload zone; upload zone is compact.

    3. Open docs/REGRESSION_TEST.md and check off items as you go. Fill in the "QA sign-off (Ops)" section.

    4. Optionally run the regression script against TestData:
       `python backend/scripts/regression_test.py --test-data "C:\Users\omack\Downloads\TestData"`
       (from repo root; requires backend/.env with valid DATABASE_URL)

    Expected: All visual items pass. Regression script runs without crashing (even if some diffs are found for data reasons).
  </how-to-verify>
  <resume-signal>Type "approved" when verified, or describe any visual issues found</resume-signal>
</task>

</tasks>

<verification>
- Frontend build passes (npm run build exits 0)
- docs/REGRESSION_TEST.md has Claude dry-run line filled in
- Human approves visual verification
</verification>

<success_criteria>
- All UI fixes from Plans 01-02 pass visual inspection
- REGRESSION_TEST.md is complete and has Claude dry-run sign-off
- Regression script is functional (no crashes on --help or syntax check)
- Ops has signed off on QA environment (or deferred to next session)
</success_criteria>

<output>
After completion, create `.planning/phases/11-refing-ui-for-regression-testing/11-05-SUMMARY.md`
</output>
