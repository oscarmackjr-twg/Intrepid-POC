---
phase: 11-refing-ui-for-regression-testing
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-03-14T21:25:59.052Z
---

<current_state>
Phase 11, Plan 05 — paused at Task 2 (human-verify checkpoint). Claude's dry-run (Task 1) completed and committed on 2026-03-13. Waiting for Ops to do browser verification against the QA environment and fill in the sign-off section of docs/REGRESSION_TEST.md.

This session was diagnostic-only: investigated 95th run regression test failures (missing loans in borrowing_file_cibc.xlsx). Root cause identified and saved to memory — no code changes made.
</current_state>

<completed_work>

- Task 1 (auto): Claude dry-run — DONE (commit 20f6669, 2026-03-13)
  - Frontend build clean (npm run build, 0 errors)
  - All 6 nav active-state conditions confirmed unique in Layout.tsx
  - max-w-5xl on ProgramRuns.tsx and FileManager.tsx confirmed
  - File list before upload zone in FileManager confirmed
  - regression_test.py syntax OK
  - docs/REGRESSION_TEST.md signed off with PASS + date + 7-item notes block
</completed_work>

<remaining_work>

- Task 2 (checkpoint:human-verify): Ops browser verification of qa.oscarmackjr.com — NOT DONE
  - Open app in browser, work through REGRESSION_TEST.md sections 1-7
  - Specifically: nav active-state (Section 2), Program Runs width (Section 4), FileManager layout (Section 5)
  - Fill in "QA sign-off (Ops)" line in docs/REGRESSION_TEST.md
  - Optionally run: `python backend/scripts/regression_test.py --test-data "C:\Users\omack\Downloads\TestData"`
  - Type "approved" to unblock and complete the phase
</remaining_work>

<decisions_made>

- Claude dry-run PASS based on: build clean, unique nav conditions, layout fixes confirmed in code
- Visual/interactive items deferred to Ops browser verification (cannot be verified programmatically)
- 95th run diagnostic: root cause of missing CIBC borrowing file loans is tagging.py greedy SG allocation — these loans fall within the SG budget window for the 95th run's loan universe (different total balances → different cutoff point in shuffled list). Saved to memory.
</decisions_made>

<blockers>

- Task 2 is a blocking human-verify checkpoint: needs Ops browser sign-off before phase can close
- 95th run regression failures: not a code bug per se — golden expected output may need to be regenerated for the 95th test case, OR loans need an explicit CIBC override in tagging.py
</blockers>

<context>
Phase 11 is essentially done on the code side. All Plans 01-05 complete except the Ops QA gate. The regression test harness (Plan 04) is working but the 95th run test case has data-driven failures because the tagging algorithm assigns different loans to SG/CIBC when run against a new loan universe.

Next natural step after Ops signs off: close phase 11 and move on (or address the 95th run regression golden data issue first).
</context>

<next_action>
Ask Ops to do browser verification: `cd frontend && npm run dev`, work through docs/REGRESSION_TEST.md sections 1-7, then type "approved" to complete the phase. If tackling the 95th run regression failures instead, check output/tagging_allocation.xlsx from the 95th run to confirm the 4 missing loans have final='sg', then decide whether to regenerate golden output or add a CIBC override.
</next_action>
