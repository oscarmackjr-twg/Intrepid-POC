---
phase: 11-refing-ui-for-regression-testing
plan: "02"
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - frontend/src/pages/ProgramRuns.tsx
  - frontend/src/pages/FileManager.tsx
autonomous: true
requirements: [UI-07]

must_haves:
  truths:
    - "Program Runs page uses a constrained max-width content area — content does not stretch edge-to-edge on wide screens"
    - "Output Directory section on Program Runs is visually separated from the Run Program controls with clear heading hierarchy"
    - "File Manager upload drop zone is compact — it does not dominate the page with a large empty box when content is present"
    - "File Manager file list section appears first (above fold) with upload zone collapsed or below"
    - "Both pages preserve the bg-white shadow rounded-lg card pattern"
  artifacts:
    - path: "frontend/src/pages/ProgramRuns.tsx"
      provides: "Restructured Program Runs layout with max-width and section ordering"
    - path: "frontend/src/pages/FileManager.tsx"
      provides: "Restructured File Manager layout with compact upload zone"
  key_links:
    - from: "frontend/src/components/Layout.tsx"
      to: "frontend/src/pages/ProgramRuns.tsx"
      via: "<Outlet /> rendering inside <div className='p-6'>"
      pattern: "p-6"
---

<objective>
Restructure ProgramRuns.tsx and FileManager.tsx for better whitespace and layout now that the 240px sidebar takes horizontal space.

Purpose: With the Phase 10 sidebar in place, both pages were designed for a full-width layout. Program Runs needs a max-width constraint and better section separation. File Manager's giant upload zone above the file list pushes content below the fold — invert the order so the file list is immediately visible.

Output: Both pages with max-width containers, improved section hierarchy, and compact upload zone.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-refing-ui-for-regression-testing/11-CONTEXT.md
@.planning/phases/11-refing-ui-for-regression-testing/11-01-SUMMARY.md

<interfaces>
<!-- Layout.tsx main content wrapper — context for width constraints -->
<!-- Main content area: <main className="flex-1 bg-[#f8fafc] min-h-screen"> -->
<!-- Inner wrapper: <div className="p-6"><Outlet /></div> -->
<!-- flex-1 means it takes remaining width after the 240px sidebar -->
<!-- On a 1440px screen: content area = 1200px. On 1280px: 1040px. -->
<!-- A max-w-5xl (1024px) or max-w-6xl (1152px) container inside keeps content readable -->

<!-- ProgramRuns.tsx outer wrapper: <div className="px-4 py-6 sm:px-0"> -->
<!-- FileManager.tsx outer wrapper: <div className="px-4 py-6 sm:px-0"> -->

<!-- Card pattern used on all sections: bg-white shadow rounded-lg -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure ProgramRuns.tsx layout</name>
  <files>frontend/src/pages/ProgramRuns.tsx</files>
  <action>
Apply layout restructuring to ProgramRuns.tsx. Make only Tailwind class changes — do NOT modify any logic, state, API calls, or JSX structure.

Changes:

1. **Outer wrapper**: Change `<div className="px-4 py-6 sm:px-0">` to `<div className="max-w-5xl">`. This constrains content to 1024px max within the `p-6` main wrapper, preventing over-stretch on wide screens. Remove the `px-4 py-6 sm:px-0` since the parent Layout already provides `p-6` padding.

2. **Page heading row**: The `<div className="flex justify-between items-center mb-6">` is fine — keep as is.

3. **Run Program card**: Currently `<div className="bg-white shadow rounded-lg p-4 mb-6">`. Change inner padding from `p-4` to `p-6` for more breathing room matching other card patterns.

4. **Standard Output card**: Currently `<div className="bg-white shadow rounded-lg p-4 mb-6">`. Change `p-4` to `p-6`.

5. **Output directory card**: Currently `<div className="bg-white shadow rounded-lg overflow-hidden">` with inner `<div className="px-4 py-5 sm:p-6">`. This is already using `sm:p-6` — keep as is. Add `mb-0` to remove any trailing margin.

6. **Section label update**: The Output directory section heading is "Output directory". Change it to "Output Directory" (capital D) for consistency with "Run Program" / "Standard Output" heading capitalisation.

These are class-only changes. Do not touch any onClick handlers, state variables, useEffect hooks, or JSX element types.
  </action>
  <verify>
    <automated>cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. `max-w-5xl` present in ProgramRuns.tsx outer wrapper. Card paddings use `p-6`. Output Directory capitalised.</done>
</task>

<task type="auto">
  <name>Task 2: Restructure FileManager.tsx layout — invert section order and compact upload zone</name>
  <files>frontend/src/pages/FileManager.tsx</files>
  <action>
Restructure FileManager.tsx. Make only Tailwind class and minimal structural changes — do NOT modify upload logic, API calls, or state.

Changes:

1. **Outer wrapper**: Change `<div className="px-4 py-6 sm:px-0">` to `<div className="max-w-5xl">` — same constraint as ProgramRuns.

2. **Move upload zone below the file list**: The current order is: (a) header row, (b) current path info, (c) upload drop zone (large), (d) file list card. Reorder to: (a) header row, (b) current path info, (c) file list card, (d) upload section. This puts files immediately visible on page load.

3. **Compact the upload drop zone**: Change the upload zone from `p-8` to `p-4`. Change the text from the large "Drag and drop files here, or click to select / Supports multiple files" to a single-line compact version:
   ```tsx
   <div className="text-sm text-gray-600">
     {uploading ? 'Uploading...' : <><span className="font-medium text-[#1a3868]">Click to upload</span> or drag and drop files</>}
   </div>
   ```
   Remove the `text-lg font-medium mb-2` wrapper div. Keep the `<input>` and `<label>` unchanged.

4. **Upload section wrapping**: Wrap the upload zone in `<div className="mt-4">` to separate it from the file list above.

5. **File list card**: The existing `<div className="bg-white shadow rounded-lg overflow-hidden">` stays. Change inner `<div className="px-4 py-5 sm:p-6">` to `<div className="p-6">` for consistency.

Preserve all existing drag event handlers (handleDrag, handleDrop) — only change classes and the text content inside the upload label.
  </action>
  <verify>
    <automated>cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/frontend && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Build passes. File list appears before upload zone in JSX order. Upload zone uses p-4 (compact). max-w-5xl on outer wrapper.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0
- `grep "max-w-5xl" frontend/src/pages/ProgramRuns.tsx` — returns match
- `grep "max-w-5xl" frontend/src/pages/FileManager.tsx` — returns match
- `grep "Output Directory" frontend/src/pages/ProgramRuns.tsx` — returns match (capital D)
- File list JSX appears before upload zone JSX in FileManager.tsx (grep line numbers confirm)
</verification>

<success_criteria>
- Both pages have max-w-5xl outer wrapper
- ProgramRuns cards use p-6 padding
- FileManager file list is above the upload zone
- Upload zone is compact (p-4)
- Frontend build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/11-refing-ui-for-regression-testing/11-02-SUMMARY.md`
</output>
