---
phase: 22-credit-quality-page
plan: 03
type: execute
wave: 3
depends_on:
  - 22-01
  - 22-02
files_modified: []
autonomous: false
requirements:
  - CREDIT-01
  - CREDIT-02
  - CREDIT-03
  - CREDIT-04
  - CREDIT-05
  - CREDIT-06
  - UX-01
must_haves:
  truths:
    - "All backend tests pass including the 5 new Phase 22 tests"
    - "All 6 Credit Quality panels render correctly in the browser"
    - "Sidebar filter changes refresh all panels"
  artifacts: []
  key_links: []
---

<objective>
Run the full backend test suite, then visually verify all 6 Credit Quality panels in the browser.

Purpose: Confirm end-to-end correctness before marking Phase 22 complete. Backend tests validate API contracts; human verification confirms visual rendering and filter interactivity.
Output: Test results confirming all pass; human sign-off on visual verification.
</objective>

<execution_context>
@C:\Users\omack\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\omack\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-credit-quality-page/22-01-SUMMARY.md
@.planning/phases/22-credit-quality-page/22-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full backend test suite</name>
  <files></files>
  <read_first>
    - backend/tests/test_re_api.py (confirm all 5 new tests exist: test_delinquency_waterfall, test_risk_rating_migration, test_loans_list_has_prior_risk_rating, test_distributions_ltv_color_bands, test_distributions_dscr_color_bands)
  </read_first>
  <action>
1. Run `cd backend && python -m pytest tests/test_re_api.py -x -q` — all RE API tests must pass.
2. Run `cd backend && python -m pytest -x -q` — full backend test suite must pass (no regressions from Plan 01 changes).
3. If any test fails, investigate and fix. The fix should be minimal (e.g., assertion count adjustments from fixture changes, not logic rewrites).
  </action>
  <verify>
    <automated>cd backend && python -m pytest -x -q</automated>
  </verify>
  <acceptance_criteria>
    - cd backend && python -m pytest tests/test_re_api.py -x -q exits 0
    - cd backend && python -m pytest -x -q exits 0
    - Output includes "passed" with zero "failed" or "error" lines
  </acceptance_criteria>
  <done>Full backend test suite passes. All 5 new Phase 22 tests and all pre-existing tests green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 6 Credit Quality panels</name>
  <action>Human verifies all 6 panels render correctly in the browser at /re-dashboard/credit and confirms filter propagation works across all panels.</action>
  <what-built>Credit Quality page with 6 panels: LTV histogram (green/yellow/red bars), DSCR histogram (green/yellow/red bars), watchlist table (risk_rating 4-5 with trend arrows), delinquency waterfall (current/30/60/90/default), risk rating migration matrix (color-coded grid), and interest rate sensitivity table (6 bps scenarios).</what-built>
  <how-to-verify>
    1. Start the dev server: `cd frontend && npm run dev` (and backend: `cd backend && uvicorn api.main:app --reload`)
    2. Log in and navigate to /re-dashboard/credit (click "Credit Quality" tab)
    3. Verify Panel 1 (LTV Distribution): bar chart renders with green bars for buckets below 65%, yellow for 65-75%, red for above 75%
    4. Verify Panel 2 (DSCR Distribution): bar chart renders with green bars for above 1.4x, yellow for 1.0-1.4x, red for below 1.0x
    5. Verify Panel 3 (Watchlist): table shows loans with risk_rating 4 or 5; "Trend" column shows arrows (up=red, down=green, dash=neutral)
    6. Verify Panel 4 (Delinquency Waterfall): bar chart shows 5 buckets ordered Current, 30 DPD, 60 DPD, 90 DPD, Default with graduated colors
    7. Verify Panel 5 (Migration Matrix): grid table with row headers (prior rating) and column headers (current rating); diagonal cells are grey, deterioration cells are red-tinted, improvement cells are green-tinted
    8. Verify Panel 6 (Sensitivity Table): 6 rows showing -300, -200, -100, +100, +200, +300 bps with New WAC, Annual Interest Impact, and Impact % columns; negative impacts in red, positive in green
    9. Change a filter in the sidebar (e.g., property type) — all 6 panels should refresh with updated data
    10. Clear all filters — panels should return to showing full portfolio data
  </how-to-verify>
  <verify>Human confirms all 6 panels render correctly</verify>
  <done>Human approves all 6 panels and filter propagation</done>
  <resume-signal>Type "approved" or describe any issues with specific panel numbers</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification plan — no new code or trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | No new threats — verification only |
</threat_model>

<verification>
1. Full backend test suite green
2. Human confirms all 6 panels render correctly
3. Human confirms filter propagation works
</verification>

<success_criteria>
- Backend test suite: 0 failures
- Human approves visual verification of all 6 panels
- Filter changes propagate to all panels
</success_criteria>

<output>
After completion, create `.planning/phases/22-credit-quality-page/22-03-SUMMARY.md`
</output>
