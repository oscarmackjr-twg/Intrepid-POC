---
phase: 08-fix-staging-auth
plan: "02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - .planning/phases/05-staging-deployment/05-VERIFICATION.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements:
  - STAGE-01
  - STAGE-02
  - STAGE-03

must_haves:
  truths:
    - "CI/CD pipeline (deploy-test.yml) completes successfully after push to main"
    - "ECS service is stable with a running task using the new task definition revision (LOCAL_DEV_MODE=true)"
    - "Staging URL http://intrepid-poc-qa-alb-1332245107.us-east-1.elb.amazonaws.com loads the application login page"
    - "Admin can log in with admin / IntrepidStaging2024! and session persists (cookie is stored)"
    - "Amber STAGING banner is visible on the login page and on every authenticated page"
    - "File upload succeeds in staging without error"
    - "VERIFICATION.md written to .planning/phases/05-staging-deployment/"
    - "REQUIREMENTS.md STAGE-01, STAGE-02, STAGE-03 marked [x]"
  artifacts:
    - path: ".planning/phases/05-staging-deployment/05-VERIFICATION.md"
      provides: "Formal verification record for Phase 5 STAGE requirements"
      contains: "STAGE-01"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Updated traceability — STAGE-01/02/03 marked complete"
      contains: "[x] **STAGE-01**"
  key_links:
    - from: "deploy/terraform/qa/ecs.tf (LOCAL_DEV_MODE=true, applied in 08-01)"
      to: "ECS task definition new revision"
      via: "terraform apply (08-01) + CI/CD force-new-deployment"
      pattern: "LOCAL_DEV_MODE.*true"
    - from: "backend/auth/routes.py"
      to: "HTTP cookie"
      via: "secure=not settings.LOCAL_DEV_MODE — False when LOCAL_DEV_MODE=true"
      pattern: "secure=not settings.LOCAL_DEV_MODE"
---

<objective>
Trigger the CI/CD pipeline by pushing to main, wait for the ECS service to stabilize with the new task definition, and run the full Phase 5 smoke test checklist to formally verify STAGE-01, STAGE-02, and STAGE-03. Write the VERIFICATION.md and mark requirements complete.

Purpose: Plan 08-01 registered a new ECS task definition revision with LOCAL_DEV_MODE=true but ECS does not auto-redeploy — a force-new-deployment is needed. Pushing to main triggers deploy-test.yml which rebuilds the image (baking in VITE_APP_ENV=staging for the banner), runs Alembic migrations, and force-deploys to ECS. Once the service is stable, the smoke test checklist formally closes the staging verification gap that has been open since Phase 5 Plan 03.

Output:
- ECS service running with LOCAL_DEV_MODE=true (login works over HTTP)
- .planning/phases/05-staging-deployment/05-VERIFICATION.md (Phase 5 formal verification record)
- .planning/REQUIREMENTS.md with STAGE-01, STAGE-02, STAGE-03 marked [x]
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-fix-staging-auth/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Push to main and monitor CI/CD pipeline</name>
  <files></files>
  <action>
Before pushing, run a local lint check to ensure the docker-compose.yml change (from Plan 08-01) does not introduce ruff/mypy issues. The YAML change touches no Python code so this is a confidence check only.

```powershell
cd C:\Users\omack\Intrepid\pythonFramework\intrepid-poc
ruff check backend/
ruff format --check backend/
```

Both must pass with no errors. If issues appear, they are pre-existing (not from Phase 8 changes) — note them but do not fix; they will be caught by security-quality-gate anyway and the YAML change itself is clean.

Commit the docker-compose.yml change if not already committed from Plan 08-01, then push:

```powershell
git add deploy/docker-compose.yml
git commit -m "fix(08): add LOCAL_DEV_MODE=true to docker-compose app environment"
git push origin main
```

Monitor the GitHub Actions pipeline:

```powershell
gh run list --workflow=deploy-test.yml --limit=3
gh run watch
```

The pipeline has these sequential jobs:
1. `security-quality-gate` — ruff, mypy, pip-audit, npm audit, terraform validate, TruffleHog (must pass to unblock deploy)
2. `build-and-push` — builds Docker image with `--build-arg VITE_APP_ENV=staging`, pushes to ECR
3. `deploy` — runs Alembic migration as ECS one-off task, then force-new-deployment, then waits for services-stable

**Expected duration:** 8-15 minutes total.

**If security-quality-gate fails:** Check which check failed in the GitHub Actions UI. If it is a pre-existing issue unrelated to Phase 8 changes, report it — do not attempt to fix lint/type issues in this plan.

**If deploy job fails after migration step:** Check ECS task exit code in CloudWatch:
```powershell
aws logs describe-log-streams `
  --log-group-name /ecs/intrepid-poc-qa `
  --order-by LastEventTime `
  --descending `
  --max-items 5 `
  --region us-east-1
```

After pipeline completes, confirm ECS service is stable with the new revision:
```powershell
aws ecs describe-services `
  --cluster intrepid-poc-qa `
  --services intrepid-poc-qa `
  --query "services[0].{taskDef:taskDefinition,running:runningCount,desired:desiredCount,status:status}" `
  --region us-east-1
```

Expected: `runningCount` = 1, `desiredCount` = 1, `status` = "ACTIVE", and `taskDef` ARN ends with the new revision number (higher than what was shown in Plan 08-01 post-apply check).

Optionally wait for stability explicitly:
```powershell
aws ecs wait services-stable `
  --cluster intrepid-poc-qa `
  --services intrepid-poc-qa `
  --region us-east-1
```
  </action>
  <verify>
    <automated>aws ecs describe-services --cluster intrepid-poc-qa --services intrepid-poc-qa --query "services[0].runningCount" --region us-east-1</automated>
  </verify>
  <done>GitHub Actions pipeline completes all jobs with green status; ECS service shows runningCount=1 with the new task definition revision; gh run list shows most recent deploy-test.yml run as "completed / success"</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full staging smoke test (STAGE-01, STAGE-02, STAGE-03)</name>
  <what-built>CI/CD has deployed a new ECS task with LOCAL_DEV_MODE=true. The login cookie is now set with secure=False, allowing it to be stored over the HTTP ALB. The StagingBanner is baked into the frontend bundle via VITE_APP_ENV=staging.</what-built>
  <how-to-verify>
Open a browser and work through the checklist below. Check every item. The staging URL is:

**http://intrepid-poc-qa-alb-1332245107.us-east-1.elb.amazonaws.com**

Admin credentials: `admin` / `IntrepidStaging2024!`

---

**STAGE-01: Staging URL loads and app renders**

- [ ] Navigate to `http://intrepid-poc-qa-alb-1332245107.us-east-1.elb.amazonaws.com` — login page loads without error
- [ ] No browser console errors indicating failed API connection
- [ ] "Backend connected" or health indicator is visible (green)

---

**STAGE-02: Ops can log in and upload a file**

- [ ] Enter `admin` / `IntrepidStaging2024!` and click Login
- [ ] Login succeeds — browser navigates to Dashboard (not back to Login, not 401)
- [ ] Navigate to File Manager
- [ ] Upload a sample .xlsx loan tape — file accepted without error, no 422/500 response

---

**STAGE-03: Staging banner is unmissable on all pages**

Check each page for the amber/yellow "STAGING — Not Production" banner:

- [ ] Login page — banner visible before logging in
- [ ] Dashboard — banner visible after login
- [ ] Program Runs — banner visible
- [ ] Pipeline Runs — banner visible
- [ ] Exceptions — banner visible
- [ ] Rejected Loans — banner visible
- [ ] File Manager — banner visible
- [ ] Cash Flow — banner visible
- [ ] Holiday Maintenance — banner visible
- [ ] Banner is sticky (scroll down on any long page — banner stays at top or bottom, does not scroll away)

---

**Troubleshooting if login fails after a successful deploy:**

1. Open browser DevTools → Application → Cookies — confirm a cookie is set after clicking Login. If no cookie is present, LOCAL_DEV_MODE may not have taken effect in the running container. Check:
   ```powershell
   aws ecs describe-task-definition `
     --task-definition intrepid-poc-qa `
     --query "taskDefinition.containerDefinitions[0].environment[?name=='LOCAL_DEV_MODE']" `
     --region us-east-1
   ```
   Must return `[{"name": "LOCAL_DEV_MODE", "value": "true"}]`. If it returns `[]`, the terraform apply from Plan 08-01 did not take effect — stop and report.

2. If cookie is present but all pages return 401, check CloudWatch for FastAPI errors:
   ```powershell
   aws logs describe-log-streams --log-group-name /ecs/intrepid-poc-qa --order-by LastEventTime --descending --max-items 3 --region us-east-1
   ```

3. If banner is missing, confirm the CI/CD build used `--build-arg VITE_APP_ENV=staging`. Check the GitHub Actions build job logs for the docker build step.
  </how-to-verify>
  <resume-signal>Type "smoke test passed" if all checklist items are checked, or describe which items failed and any error messages observed</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Write VERIFICATION.md and mark requirements complete</name>
  <files>
    .planning/phases/05-staging-deployment/05-VERIFICATION.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
Only execute this task after the smoke test checkpoint (Task 2) has been approved with "smoke test passed".

**Step 1: Write .planning/phases/05-staging-deployment/05-VERIFICATION.md**

Create the file with this structure:

```markdown
# Phase 5: Staging Deployment — Verification

**Verified:** {today's date}
**Verifier:** Human smoke test (Phase 8, Plan 08-02)
**Status:** PASSED

## Requirements Verified

| Requirement | Description | Result | Evidence |
|-------------|-------------|--------|----------|
| STAGE-01 | Staging URL is accessible and app loads after CI/CD deploy | PASS | Login page loaded at http://intrepid-poc-qa-alb-1332245107.us-east-1.elb.amazonaws.com |
| STAGE-02 | Ops team can log in and upload a file in staging | PASS | Admin logged in with admin/IntrepidStaging2024!; sample .xlsx upload accepted |
| STAGE-03 | Staging environment has an unmissable banner (not production) | PASS | Amber "STAGING — Not Production" banner visible on Login page and all 8 authenticated pages; banner sticky on scroll |

## Gap Closure

This verification closes the gap created when Phase 5 Plan 03 stalled due to the Secure cookie
failure over HTTP ALB (MISS-02). The root cause was LOCAL_DEV_MODE=false in the ECS task
definition, causing FastAPI to set secure=True on the login cookie. HTTP ALB silently drops
secure cookies; the session was never stored.

**Fix applied in Phase 8:**
- Plan 08-01: Added LOCAL_DEV_MODE=true to deploy/docker-compose.yml (MISS-01); terraform
  applied to register new ECS task definition revision with LOCAL_DEV_MODE=true (MISS-02)
- Plan 08-02: CI/CD triggered via push to main; ECS force-new-deployment picked up new task
  def; smoke test confirmed login, upload, and banner functional

## Verification Notes

- StagingBanner was implemented in Phase 5 Plan 01 (VITE_APP_ENV=staging baked into Docker image
  at build time via ARG/ENV in Dockerfile + --build-arg in deploy-test.yml)
- Staging admin user was seeded in Phase 5 Plan 02 (seed_staging_user.py); user persists in RDS
- ALB URL: http://intrepid-poc-qa-alb-1332245107.us-east-1.elb.amazonaws.com
- ECS cluster: intrepid-poc-qa, service: intrepid-poc-qa, region: us-east-1
```

**Step 2: Update .planning/REQUIREMENTS.md**

Read the current REQUIREMENTS.md. Find these three lines in the Staging section and change `[ ]` to `[x]`:

```
- [ ] **STAGE-01**: Staging URL is accessible and app loads after CI/CD deploy
```
becomes:
```
- [x] **STAGE-01**: Staging URL is accessible and app loads after CI/CD deploy
```

STAGE-02 and STAGE-03 are already `[x]` per the current REQUIREMENTS.md — verify they remain `[x]` (do not change them back).

Also update the traceability table at the bottom of REQUIREMENTS.md. Find these rows and change status from "Pending" to "Complete":

```
| STAGE-01 | Phase 8 (gap closure) | Pending |
| STAGE-02 | Phase 8 (gap closure) | Pending |
| STAGE-03 | Phase 8 (gap closure) | Pending |
```

becomes:

```
| STAGE-01 | Phase 8 (gap closure) | Complete |
| STAGE-02 | Phase 8 (gap closure) | Complete |
| STAGE-03 | Phase 8 (gap closure) | Complete |
```

**Step 3: Commit the verification files**

```powershell
cd C:\Users\omack\Intrepid\pythonFramework\intrepid-poc
git add .planning/phases/05-staging-deployment/05-VERIFICATION.md
git add .planning/REQUIREMENTS.md
git commit -m "docs(08): write Phase 5 VERIFICATION.md and mark STAGE-01/02/03 complete"
git push origin main
```
  </action>
  <verify>
    <automated>Select-String -Path .planning/phases/05-staging-deployment/05-VERIFICATION.md -Pattern "STAGE-01" | Select-Object -First 1</automated>
  </verify>
  <done>.planning/phases/05-staging-deployment/05-VERIFICATION.md exists and contains STAGE-01/02/03 PASS records; .planning/REQUIREMENTS.md shows [x] for STAGE-01, STAGE-02, STAGE-03; all three traceability rows show "Complete"; changes committed and pushed</done>
</task>

</tasks>

<verification>
Overall phase checks after all tasks complete:

1. `gh run list --workflow=deploy-test.yml --limit=1` shows most recent run as "completed / success"
2. `aws ecs describe-services --cluster intrepid-poc-qa --services intrepid-poc-qa --query "services[0].runningCount" --region us-east-1` returns 1
3. `.planning/phases/05-staging-deployment/05-VERIFICATION.md` exists with all three requirements showing PASS
4. `.planning/REQUIREMENTS.md` shows `[x] **STAGE-01**` in the Staging section
5. Traceability table rows for STAGE-01, STAGE-02, STAGE-03 all show "Complete"
6. Human smoke test approved (Task 2 checkpoint passed)
</verification>

<success_criteria>
- CI/CD pipeline completes without failures (all jobs green: security-quality-gate, build-and-push, deploy)
- ECS service running with LOCAL_DEV_MODE=true task definition; runningCount=1
- All STAGE-01 smoke test items pass: URL loads, login succeeds, session cookie stored
- All STAGE-02 smoke test items pass: file upload accepted
- All STAGE-03 smoke test items pass: banner on every page, sticky on scroll
- 05-VERIFICATION.md written to correct path (.planning/phases/05-staging-deployment/)
- REQUIREMENTS.md updated: STAGE-01 marked [x], traceability table shows Complete for all three STAGE requirements
- Phase 8 complete — all three STAGE requirements formally verified
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-staging-auth/08-02-SUMMARY.md` following the summary template at @C:/Users/omack/.claude/get-shit-done/templates/summary.md.

Include:
- CI/CD pipeline run ID and conclusion
- ECS task definition revision number (post-deploy)
- Smoke test result (all items passed / any items that needed investigation)
- Confirmation that VERIFICATION.md and REQUIREMENTS.md are written and pushed
</output>
