---
phase: 06-final-funding-cashflow-integration
plan: 05
type: execute
wave: 4
depends_on: [06-04]
files_modified: []
autonomous: false
requirements:
  - FF-01
  - FF-02
  - FF-03
  - FF-04
  - FF-05
  - FF-06
  - FF-07
  - FF-08
  - FF-09

must_haves:
  truths:
    - "Final Funding SG run shows QUEUED then RUNNING then COMPLETED in the Program Runs UI without a page refresh"
    - "Final Funding CIBC run shows QUEUED then RUNNING then COMPLETED in the Program Runs UI without a page refresh"
    - "Output Excel files appear in the file manager after a COMPLETED run"
    - "Running the full test suite passes (all non-integration tests green)"
    - "Automated tests for FF-03 through FF-09 pass"
  artifacts:
    - path: "backend/tests/test_final_funding_jobs.py"
      provides: "All 5 test functions pass or skip (no failures)"
    - path: "backend/tests/test_final_funding_runner.py"
      provides: "FF-07 and FF-08 tests pass"
  key_links:
    - from: "ProgramRuns.tsx button click"
      to: "final_funding_job DB row"
      via: "POST /api/program-run/jobs → background thread → DB update → GET /api/program-run/jobs/{id} poll"
      pattern: "status.*COMPLETED"
---

<objective>
Run the full test suite to verify all FF requirements are green, then perform a human end-to-end smoke test: trigger Final Funding SG and CIBC from the Program Runs UI and confirm live status polling works.

Purpose: Automated tests verify the backend logic; the human checkpoint verifies the UI polling, output file appearance, and real-script execution end-to-end in the running environment.
Output: All tests green. Human confirms RUNNING → COMPLETED status visible in UI without page refresh. Phase 6 complete.
</objective>

<execution_context>
@C:/Users/omack/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/omack/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-final-funding-cashflow-integration/06-CONTEXT.md
@.planning/phases/06-final-funding-cashflow-integration/06-VALIDATION.md
@.planning/phases/06-final-funding-cashflow-integration/06-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm FF tests pass</name>
  <files></files>
  <action>
    Run the full backend test suite:

    ```
    cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend
    python -m pytest tests/ -v --ignore=tests/test_integration_pipeline.py 2>&1
    ```

    The `test_integration_pipeline.py` exclusion prevents the slow full-pipeline test from blocking the gate; run it separately if needed.

    Specifically confirm:
    - `tests/test_final_funding_jobs.py` — all 5 tests PASS or SKIP (no FAIL)
    - `tests/test_final_funding_runner.py` — FF-07 and FF-08 tests PASS; FF-01, FF-02 SKIP (integration, require real files)

    If any test fails unexpectedly, fix the implementation and re-run before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && python -m pytest tests/test_final_funding_jobs.py tests/test_final_funding_runner.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>test_final_funding_jobs.py: 5 tests PASSED or SKIPPED (0 failed). test_final_funding_runner.py: test_cashflow_bridge_copies_file and test_cashflow_bridge_absent_is_noop PASSED. test_sg_script_executes and test_cibc_script_executes SKIPPED (integration).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Phase 6 complete backend and frontend changes:
    - Real Final Funding SG and CIBC scripts bundled at backend/scripts/ (FOLDER env, no hardcoded paths)
    - Cashflow bridge: current_assets.csv auto-copied from outputs area into Final Funding temp inputs
    - final_funding_job DB table with QUEUED/RUNNING/COMPLETED/FAILED lifecycle
    - POST /api/program-run/jobs and GET /api/program-run/jobs/{job_id} endpoints
    - ProgramRuns.tsx: inline status polling replaces alert() for both Final Funding buttons
  </what-built>
  <how-to-verify>
    1. Start the local stack: `docker compose -f deploy/docker-compose.yml up` (or `uvicorn` + `npm run dev` directly)

    2. Open the app in a browser and log in.

    3. Navigate to Program Runs.

    4. Click "Final Funding SG":
       - Status "QUEUED" should appear immediately beneath the button (no page freeze)
       - Status should update to "RUNNING..." within ~5 seconds
       - Status should update to "COMPLETED" when the script finishes (may take 1-5 minutes for real scripts)
       - Output files should appear in the file manager under final_funding_sg/ prefix automatically

    5. Click "Final Funding CIBC" and observe the same QUEUED → RUNNING → COMPLETED progression.

    6. Optional: click one of the buttons a second time while a job is running for the same mode — confirm an inline error message (not an alert popup) says a job is already running (409).

    7. Confirm no `alert()` popups appear for Final Funding SG or CIBC completion or failure (only inline status text).

    Note: If input files are not present in files_required/, the job will reach FAILED status with an error message. That is expected behavior — the key thing to verify is that the status display works (QUEUED → RUNNING → FAILED is also a valid verification path if input files are not available).
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 6 complete, or describe any issues observed.</resume-signal>
</task>

</tasks>

<verification>
Full automated gate:
  cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && python -m pytest tests/ -v --ignore=tests/test_integration_pipeline.py 2>&1 | tail -20

Manual gate: Human confirms inline status polling visible in Program Runs UI.
</verification>

<success_criteria>
- All non-integration tests pass (0 failures)
- FF-07 and FF-08 (bridge tests) pass
- FF-03, FF-06, FF-09 (job API tests) pass
- Human confirms: QUEUED → RUNNING → COMPLETED/FAILED status visible in UI without page refresh
- Human confirms: no alert() popup for Final Funding SG or CIBC completion
- Human confirms: output files appear in file manager on COMPLETED (or FAILED message appears inline on failure)
</success_criteria>

<output>
After completion, create `.planning/phases/06-final-funding-cashflow-integration/06-05-SUMMARY.md`
</output>
