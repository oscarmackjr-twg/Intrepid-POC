---
phase: 15-integrate-updated-tagging-logic
plan: 2
task: 3
total_tasks: 3
status: in_progress
last_updated: 2026-03-23T19:47:11.538Z
---

<current_state>
Paused at Plan 15-02, Task 3 — the human verification checkpoint. Tasks 1 and 2 of plan 15-02 are complete (regression tests ran + re-baselined, docs updated). Waiting for user to approve the checkpoint.

The checkpoint is specifically about regression test results. The content diffs (ratio change) have been re-baselined. There is a pre-existing structural issue with 38 "missing" files in the golden directory that needs a decision.
</current_state>

<completed_work>

**Plan 15-01 — COMPLETE (commits: f6e37c9, 66514a6, 7a69480)**
- Task 1: Extracted `allocate_sg(buy_df, grouped_sum, p=0.325, s=0.5)` from tagging.py
  - Added `if __name__ == "__main__":` guard for importability
  - Replaced hardcoded 12-key SG dict with dynamic loop over `grouped_sum.index`
  - Updated ratios: p=0.3→0.325, s=0.6→0.5
  - Added `.get(tag, 0)` KeyError guard
  - Updated inline comment to "50% of SFY, 32.5% of PRIME"
- Task 2: Created `backend/tests/test_tagging_allocation.py` with 7 tests — all pass
- SUMMARY.md, STATE.md, ROADMAP.md all updated

**Plan 15-02 — Tasks 1 & 2 complete (commit: 0556864)**
- Task 1 (regression):
  - Updated sg/cibc splits in TestData/93rd_buy/files_required and 95th_buy/files_required by running tagging.py with new ratios (93rd: SG=650, CIBC=832; 95th: SG=443, CIBC=573)
  - Ran `regression_test_funding.py --update-golden` twice — wrote 23 files each time for 93rd and 95th
  - Content diffs from ratio change are now re-baselined
- Task 2 (docs): Updated developer reference Word doc (new section 5.9 with 50% SFY, 32.5% PRIME, dynamic loop description) and tests/README.md (added test_tagging_allocation.py entry)
- Task 3: BLOCKED at human checkpoint
</completed_work>

<remaining_work>

- Task 3 (15-02): Human verification checkpoint
  - User reviews regression results and docs
  - User types "approved" or reports issues
  - Then: phase verifier runs, ROADMAP/STATE updated, phase marked complete
</remaining_work>

<decisions_made>

- Added `if __name__ == "__main__":` guard — script ran on import with FileNotFoundError; guard was required for unit tests to import allocate_sg
- Re-ran tagging.py directly on TestData before re-baselining — TestData/files_required had old sg/cibc splits; pipeline regression reruns Phase 2 (tagging) with new ratios, so funding golden files had to be based on new splits to match
- Multiple `--update-golden` runs were needed — first used old splits (wrong baseline), second used new splits (correct)
</decisions_made>

<blockers>

**Structural regression test issue (pre-existing, not caused by Phase 15):**

The funding regression test (`regression_test_funding.py`) shows:
- 93rd_buy: 0 differ, 38 missing (after re-baselining)
- 95th_buy: 0 differ, 23 missing (after re-baselining)
- 98th_buy: FAIL — FundingSG crashes (missing tape file, pre-existing)

The "missing" files are in the golden directory (`TestData/93rd_buy/output/`) but are never produced by the funding-only test. They include: pre-funding outputs (flagged_loans, comap_not_passed, purchase_price_mismatch, loans_data) and tagging outputs (tagging_summary, tagging_allocation). These have been in the golden directory from previous full pipeline runs — not introduced by Phase 15.

The pipeline regression test (`regression_test.py`) also shows diffs for 93rd/95th — same root cause.

**Decision needed:** Accept pre-existing state (38 missing is not Phase 15's fault) or clean up golden directory.
</blockers>

<context>

The Phase 15 CODE changes are 100% correct and verified:
- `allocate_sg` function works, is importable, has correct ratios
- 255 tests pass (7 new + 248 existing)
- Developer reference docs updated
- Tests README updated

The regression test issue is purely a golden file maintenance problem. The user should decide whether to:
1. Approve the checkpoint accepting the pre-existing regression state
2. Clean the golden directories first (remove non-funding files from TestData/output/), then re-run --update-golden, then approve

Background tasks that were running when paused (can be ignored):
- bblxssi3z: funding test with --no-cleanup
- bo3y7k62y: another --update-golden run
Their results don't matter — run a fresh clean test when resuming.
</context>

<next_action>
1. Run: `cd C:/Users/omack/Intrepid/pythonFramework/intrepid-poc/backend && python scripts/regression_test_funding.py 2>&1 | grep -E "BUY DATE|SUMMARY|PASSED|FAILED|differ|missing"`
2. Expected: 93rd=0 differ/38 missing, 95th=0 differ/23 missing, 98th=pre-existing crash
3. Decide: type "approved" to close checkpoint (accepting pre-existing regression state) or fix golden dir first
4. After "approved": orchestrator spawns verifier → ROADMAP/STATE updated → phase 15 complete
</next_action>
