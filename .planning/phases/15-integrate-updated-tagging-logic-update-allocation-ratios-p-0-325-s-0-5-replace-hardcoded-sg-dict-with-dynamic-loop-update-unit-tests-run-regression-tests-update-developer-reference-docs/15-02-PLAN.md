---
phase: 15-integrate-updated-tagging-logic
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - docs/Intrepid_Platform_Developer_Reference.docx
  - backend/tests/README.md
autonomous: false
requirements:
  - TAG-06

must_haves:
  truths:
    - "Regression tests pass (or golden files are re-baselined if only ratio-change diffs)"
    - "Developer reference docs reflect new ratios (50% SFY, 32.5% PRIME) and dynamic loop"
    - "Tests README lists the new test_tagging_allocation.py file"
  artifacts:
    - path: "docs/Intrepid_Platform_Developer_Reference.docx"
      provides: "Updated developer reference with new allocation ratios"
    - path: "backend/tests/README.md"
      provides: "Updated test listing"
      contains: "test_tagging_allocation.py"
  key_links: []
---

<objective>
Run regression tests against sample data to validate the ratio changes produce expected output differences, re-baseline golden files if needed, and update the developer reference Word doc and tests README to reflect the new allocation logic.

Purpose: Ensure the tagging changes don't break downstream pipeline steps and that documentation stays current.
Output: Validated regression results + updated docs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-integrate-updated-tagging-logic-update-allocation-ratios-p-0-325-s-0-5-replace-hardcoded-sg-dict-with-dynamic-loop-update-unit-tests-run-regression-tests-update-developer-reference-docs/15-CONTEXT.md
@.planning/phases/15-integrate-updated-tagging-logic-update-allocation-ratios-p-0-325-s-0-5-replace-hardcoded-sg-dict-with-dynamic-loop-update-unit-tests-run-regression-tests-update-developer-reference-docs/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run regression tests and re-baseline if needed</name>
  <files>backend/scripts/regression_test.py, backend/scripts/regression_test_funding.py</files>
  <read_first>
    - backend/scripts/regression_test.py (first 50 lines — understand CLI args and TestData path)
    - backend/scripts/regression_test_funding.py (first 50 lines — understand --update-golden flag)
    - backend/scripts/tagging.py (confirm allocate_sg changes from Plan 01 are in place)
    - .planning/phases/15-integrate-updated-tagging-logic-update-allocation-ratios-p-0-325-s-0-5-replace-hardcoded-sg-dict-with-dynamic-loop-update-unit-tests-run-regression-tests-update-developer-reference-docs/15-CONTEXT.md (decision D-13)
  </read_first>
  <action>
    **Per D-13:** Run the full regression test suite and the funding-only regression test against sample data.

    Step 1: Run the full pipeline regression test:
    ```
    cd backend && python scripts/regression_test.py
    ```

    Step 2: Examine the output. Diffs are expected in tagging output files (tagging_summary.xlsx, tagging_allocation.xlsx, and the _sg/_cibc split exhibit files) because the allocation ratios changed. Downstream funding/cashflow files may also differ if the SG/CIBC loan split changed.

    Step 3: If diffs are ONLY due to the ratio change (different loan counts in SG vs CIBC splits, different allocation targets in summary), these are expected. Re-baseline the golden files:
    ```
    cd backend && python scripts/regression_test_funding.py --update-golden
    ```

    Step 4: Run both regression tests again to confirm they pass after re-baselining:
    ```
    cd backend && python scripts/regression_test.py
    cd backend && python scripts/regression_test_funding.py
    ```

    If any unexpected diffs appear (e.g., in pre-funding eligibility outputs, date calculations, or fields unrelated to tagging allocation), investigate and report — do NOT re-baseline those.

    Note: TestData folder location is auto-discovered by the regression scripts. If not found, check MEMORY.md for path hints or skip with a note.
  </action>
  <verify>
    <automated>cd backend && python scripts/regression_test.py 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `regression_test.py` exits with "ALL PASSED" or equivalent success message
    - `regression_test_funding.py` exits with success after re-baselining
    - No unexpected diffs in pre-funding or eligibility outputs
  </acceptance_criteria>
  <done>Both regression test scripts pass; golden files re-baselined if ratio-change diffs were the only differences</done>
</task>

<task type="auto">
  <name>Task 2: Update developer reference docs and tests README</name>
  <files>docs/Intrepid_Platform_Developer_Reference.docx, backend/tests/README.md</files>
  <read_first>
    - docs/Intrepid_Platform_Developer_Reference.docx (find sections mentioning tagging, allocation ratios, SG/CIBC split)
    - backend/tests/README.md (see current test file listing)
  </read_first>
  <action>
    **Per D-12:** Update `docs/Intrepid_Platform_Developer_Reference.docx`:
    - Find any mention of "60% of SFY" or "30% of PRIME" allocation ratios and change to "50% of SFY" and "32.5% of PRIME"
    - Find any mention of hardcoded SG dictionary or fixed tag list and update to describe the dynamic loop: "SG allocation targets are computed dynamically from all tags in the buy tape. Tags prefixed with SFY use ratio s=0.5, tags prefixed with PRIME use ratio p=0.325, and tags ending with _bd always receive 0 allocation (all bad-debt loans go to CIBC)."
    - If the doc mentions specific tag names (SFYstandard, PRIMEwpdi, etc.) as a fixed list, update to note these are dynamically derived from the buy tape data, not hardcoded

    Update `backend/tests/README.md`:
    - Add `test_tagging_allocation.py` to the test file listing in the Test Structure section, between `test_storage_local.py` and `README.md` (alphabetical), with description: `# Tagging SG allocation logic (Phase 15)`
    - The entry should look like: `├── test_tagging_allocation.py         # Tagging SG allocation logic (Phase 15)`
  </action>
  <verify>
    <automated>grep -c "test_tagging_allocation" backend/tests/README.md</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/README.md contains `test_tagging_allocation.py`
    - docs/Intrepid_Platform_Developer_Reference.docx has been modified (file mtime updated)
    - The Word doc no longer contains "60% of SFY" or "30% of PRIME" in the tagging section
  </acceptance_criteria>
  <done>Developer reference reflects 50% SFY / 32.5% PRIME ratios and dynamic loop; tests README lists new test file</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of regression results and docs</name>
  <what-built>
    Updated tagging allocation logic (p=0.325, s=0.5, dynamic SG dict), unit tests, regression test results, and developer reference docs.
  </what-built>
  <how-to-verify>
    1. Review regression test output — confirm only tagging-related diffs were re-baselined
    2. Open `docs/Intrepid_Platform_Developer_Reference.docx` and search for "allocation" — confirm ratios show 50% SFY and 32.5% PRIME
    3. Run `cd backend && python -m pytest tests/test_tagging_allocation.py -v` — confirm all 7 tests pass
    4. Run `cd backend && python -m pytest -m "not integration" --tb=short -q` — confirm full suite passes
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `cd backend && python scripts/regression_test.py` — passes
2. `cd backend && python scripts/regression_test_funding.py` — passes
3. `grep "test_tagging_allocation" backend/tests/README.md` — found
4. Developer reference Word doc updated with new ratios
</verification>

<success_criteria>
- Regression tests pass with updated tagging logic
- Golden files re-baselined for expected ratio-change diffs
- Developer reference documents new ratios and dynamic loop behavior
- Tests README lists test_tagging_allocation.py
- Human approves all changes
</success_criteria>

<output>
After completion, create `.planning/phases/15-integrate-updated-tagging-logic-update-allocation-ratios-p-0-325-s-0-5-replace-hardcoded-sg-dict-with-dynamic-loop-update-unit-tests-run-regression-tests-update-developer-reference-docs/15-02-SUMMARY.md`
</output>
